\chapter{Curve}
    \section{Richiami generali di calcolo differenziale}
        Nel seguito considereremo lo spazio vettoriale euclideo $\mathbb{R}^n$ munito del prodotto scalare standard

        $\forall x=(x^1,\ldots,x^n) \quad y=(y^1,\ldots,y^n) \in \mathbb{R}^n$ 
        \[ x \cdot y = \sum_{i=1}^n x^i y^i \]
        \[ \norm{x} = \sqrt{x \cdot x} = \sqrt{\sum_{i=1}^n (x^i)^2} \]
        \[d(x,y) = \norm{x-y}= \sqrt{\sum_{i=1}^n (x^i - y^i)^2} \]
        \begin{definizione}
        Sia $A \subset \mathbb{R}^n$ un insieme aperto e $F: A \to \mathbb{R}^m$ una funzione, si dice che $F$ è
        \textcolor{red}{differenziabile} in $x_0 \in A$ se $\exists L: \mathbb{R}^n \to \mathbb{R}^m$ applicazione linerare tale che
        \[ \lim_{x \to x_0} \frac{F(x) - F(x_0) - L(x-x_0)}{\norm{x-x_0}} = 0 \]
        $L$ se esiste è unica e prende il nome di \textcolor{red}{differenziale di $F$ in $x_0$} e si denota con \textcolor{red}{$L=\left(dF\right)_{x_0}$}

        Si dice che $F: A \to \mathbb{R}^m$ è \textcolor{red}{differenziabile} in $A$ se è differenziabile in ogni punto di $A$.
        \end{definizione}
        \begin{esempio} \textcolor{violet}{(Un caso molto semplice di applicazione differenziabile è un'applicazione lineare)}
        
        Se $L: \mathbb{R}^n \to \mathbb{R}^m$ è un'applicazione lineare allora $L$ è differenziabile e $\left(dL\right)_{x_0}=L$ per ogni $x_0 \in \mathbb{R}^n$
  
        Perchè $\displaystyle L(x)-L(x_0)-L(x-x_0)=0 \Rightarrow \lim_{x \to x_0} \frac{L(x)-L(x_0)-L(x-x_0)}{\norm{x-x_0}} = 0$
        \end{esempio}
        \begin{esempio} 
        \textcolor{violet}{(Un altro caso semplice di applicazione differenziabile è una traslazione di vettore $b$ che in generale non è lineare, lo è solo quando $b=0$)}
        
        Sia $b \in \mathbb{R}^n$ e $\tau_b: \mathbb{R}^n \to \mathbb{R}^n$ $x \mapsto x+b$ cioè la traslazione di vettore $b$ allora 
        $\tau_b$ è differenziabile e $\left(d\tau_b\right)_{x_0} = id_{\mathbb{R}^n}$ per ogni $x_0 \in \mathbb{R}^n$

        Perchè $\displaystyle \tau_b(x)-\tau_b(x_0)-id_{\mathbb{R}^n}(x-x_0) =x+b-(x_0+b)-(x-x_0)=0 \Rightarrow$
        
        $\displaystyle \Rightarrow \lim_{x \to x_0} \frac{\tau_b(x)-\tau_b(x_0)-id_{\mathbb{R}^n}(x-x_0)}{\norm{x-x_0}} = 0$
        \end{esempio}
        \begin{osservazione}
        La composizione di due applicazioni differenziabili è differenziabile e vale:
            \begin{equation}
            \label{eq:composizione_di_applicazioni_differenziabili}
            \left(d(G \circ F)\right)_{x_0} = \left(dG\right)_{F(x_0)} \circ \left(dF\right)_{x_0}
            \end{equation}
        \end{osservazione}
        \begin{definizione}
        $R: \mathbb{R}^n \to \mathbb{R}^n$ si dice \textcolor{red}{isometria lineare} (o operatore unitario) se $R$ è lineare e conserva il prodotto scalare cioè
        \[ \forall x,y \in \mathbb{R}^n \quad R(x) \cdot R(y) = x \cdot y \]
        o equivalentemente conserva le norme 
        \[ \forall x \in \mathbb{R}^n \quad \norm{R(x)} = \norm{x} \]
        \end{definizione}
        \begin{proposizione}\textcolor{violet}{(vista in Geo $2$)}
        \label{prop:caratterizzazione_isometria_lineare}

        $R: \mathbb{R}^n \to \mathbb{R}^n$ è un'isometria lineare $\iff
        \begin{cases} 
           R(0_{\mathbb{R}^n})=0_{\mathbb{R}^n} \\
            \forall x,y \in \mathbb{R}^n \quad \norm{R(x)-R(y)}=\norm{x-y}
        \end{cases}$

        \textcolor{violet}{(In Geometria $2$ l'abbiamo vista in generale per ogni spazio vettoriale euclideo non solo $\mathbb{R}^n$ e ci dice
        che le isometrie lineari conservano le distanze)}
        \end{proposizione}
        \begin{definizione}
        $\phi: \mathbb{R}^n \to \mathbb{R}^n$ si dice \textcolor{red}{isometria} se $\forall x,y \in \mathbb{R}^n$ 
        $\norm{\phi(x)-\phi(y)} = \norm{x-y}$ \textcolor{violet}{(da non confondere con la precedente definizione di isometria lineare questa conserva solo le distanze tra i punti 
        quindi la prop \ref{prop:caratterizzazione_isometria_lineare} ci dice che le isometrie lineari in $\mathbb{R}^n$ sono tutte e sole le isometrie che mandano il vettore
        nullo nel vettore nullo)}
        \end{definizione}
        \begin{proposizione}
        \label{prop:caratterizzazione_isometria}
        Sia $\phi: \mathbb{R}^n \to \mathbb{R}^n$ un'isometria allore $\exists \, ! R: \mathbb{R}^n \to \mathbb{R}^n$ isometria lineare e
        $\exists \, ! b \in \mathbb{R}^n$ tale che $\phi = \tau_b \circ R$ \textcolor{violet}{(cioè $\phi(x) = R(x)+b$ ogni isometria si scrive come composizione
        di una traslazione e un'isometria lineare)}
        \end{proposizione}
        \begin{proof}
        Supponiamo che $\exists R: \mathbb{R}^n \to \mathbb{R}^n$ isometria lineare e $\exists b \in \mathbb{R}^n$ tale che $\phi = \tau_b \circ R$ 

        $\phi(0_{\mathbb{R}^n}) = \tau_b(R(0_{\mathbb{R}^n})) \overset{\text{prop.} \ref{prop:caratterizzazione_isometria_lineare}}{=} \tau_b(0_{\mathbb{R}^n}) = b \Rightarrow b = \phi(0_{\mathbb{R}^n})$
        
        Sia \textcolor{violet}{(si ottiene componendo a destra con l'inversa di $\tau_b$)} $R= \tau_{-b} \circ \phi$ verifichiamo 
        che $R$ è un'isometria lineare, $R$ è isometria in quanto composta di isometrie \textcolor{violet}{(la traslazione inversa e $\phi$)} e inoltre

        $R(0_{\mathbb{R}^n})= \tau_{-b}(\phi(0_{\mathbb{R}^n})) = \tau_{-b}(b) = 0_{\mathbb{R}^n} \overset{\text{prop.} \ref{prop:caratterizzazione_isometria_lineare}}{\Rightarrow} R$ è isometria lineare.
        
        \textcolor{violet}{In questa dimostrazione abbiamo supposto che esistano $R$ e $b$ tali che $\phi = \tau_b \circ R$ e abbiamo mostrato come determinarli esplicitamente.}
        \end{proof}
        \begin{osservazione}
        Se $\phi: \mathbb{R}^n \to \mathbb{R}^n$ è un'isometria allora $\phi$ è differenziabile e per la
        
        proposizione \ref{prop:caratterizzazione_isometria} $\phi= \tau_b \circ R$ 
        
        $\Rightarrow \left(d\phi\right)_{x_0} = \left(d \left( \tau_b \circ R \right) \right)_{x_0} \overset{\eqref{eq:composizione_di_applicazioni_differenziabili}}{=} \left(d\tau_b\right)_{R(x_0)} \circ \left(dR\right)_{x_0} = id_{\mathbb{R}^n} \circ R = R$ 
        \end{osservazione}
        \newpage
        \begin{definizione}
        Sia $F:A \to \mathbb{R}^m$ con $A \subseteq \mathbb{R}^n$ aperto si definiscono le \textcolor{red}{funzioni componenti} di $F$ 

        \textcolor{red}{$F^i = \pi^i \circ F : A \to \mathbb{R}^n$} per $i=1,\ldots,m$ dove $\pi^i: \mathbb{R}^m \to \mathbb{R}$ è la proiezione 
        $i$-esima 

        Se $F$ è differenziabile in $\displaystyle x_0 \in A \overset{\textcolor{violet}{\substack{\text{dim in} \\ \text{analisi 3} }}}{\Rightarrow} \forall i=1,\ldots,m \quad \forall j=1,\ldots,n \quad$ \scalebox{1.6}{$\exists$} $\displaystyle \frac{\partial F^i}{\partial x_j}(x_0)$ \textcolor{blu}{(le $F^i$ 
        sono parzialmente differenziabili in $x_0$)}
        
        Si definisce \textcolor{red}{matrice Jacobiana di $F$ in $x_0$} la matrice 
        \[ \textcolor{red}{\text{\scalebox{1.4}{\text{$J_F(x_0)$}}}} = \begin{pmatrix} \frac{\partial F^1}{\partial x_1}(x_0) & \ldots & \frac{\partial F^1}{\partial x_n}(x_0) \\
        \vdots & \ddots & \vdots \\
        \frac{\partial F^m}{\partial x_1}(x_0) & \ldots & \frac{\partial F^m}{\partial x_n}(x_0) \end{pmatrix} = \left( \frac{\partial F^i}{\partial x_j}(x_0) \right)_{\substack{1 \leq i \leq m \\ 1 \leq j \leq n}} \in \scalebox{1.4}{\text{$M_{m,n}(\mathbb{R})$}} \]
        La matrice $J_F(x_0)$ è la matrice associata a $\left(dF\right)_{x_0}:R^n \to R^m$ rispetto alle basi canoniche di $\mathbb{R}^n$ e $\mathbb{R}^m$ 
        dove $\left\{e_j\right\}_{j=1,\ldots,n}$ è la base canonica di $\mathbb{R}^n$ e
        $\left\{e'_i\right\}_{i=1,\ldots,m}$ è la base canonica di $\mathbb{R}^m$
        
        $\displaystyle dF_{x_0}(e_j) = \sum_{i=1}^m \frac{\partial F^i}{\partial x_j}(x_0) e'_i \overset{\textcolor{violet}{\substack{j\text{-esima} \\ \text{colonna della} \\ \text{Jacobiana}}}}{=} \begin{pmatrix}
        \frac{\partial F^1}{\partial x_j}(x_0) \\
        \vdots \\
        \frac{\partial F^m}{\partial x_j}(x_0)
        \end{pmatrix}$
        
        Si dice che $F:A \to \mathbb{R}^m$ è \textcolor{red}{differenziabile di classe $C^1$} se ammette derivate parziali in ogni punto di $A$ e 
        $\displaystyle \frac{\partial F^1}{\partial x_j}:A \to \mathbb{R}$ sono continue.

        Si dice che $F:A \to \mathbb{R}^m$ è \textcolor{red}{differenziabile di classe $C^k$} se ammette derivate parziali continue di ordine $k$ in ogni punto di $A$

        Si dice che $F:A \to \mathbb{R}^m$ è \textcolor{red}{differenziabile di classe $C^\infty$} se è di classe $C^k \quad \forall k \geq 1$ \textcolor{violet}{(in questo corso
        lavoreremo solo con funzioni di classe $C^\infty$ quindi ogni volta che diremo applicazione differenziabile ci riferiremo a una funzione di classe $C^\infty$)} una funzione di classe $C^\infty$ è detta anche \textcolor{red}{liscia} o \textcolor{red}{smooth}.
        \end{definizione}
    \section{Spazio tangente e fibrato di \texorpdfstring{$\mathbb{R}^n$}{R\textasciicircum n}}
        \begin{definizione}
        Sia $p \in \mathbb{R}^n$ definiamo \textcolor{violet}{(questa definizione sarà coerente con la nozione di spazio tangente in varietà differenziabili)}
        \[ \textcolor{red}{T_p \mathbb{R}^n := \left\{p\right\} \times \mathbb{R}^n = \left\{x_p=(p,x) \mid x \in \mathbb{R}^n \right\}} \]
        $T_p \mathbb{R}^n$ possiende struttura di spazio vettoriale reale mediante le leggi di composizione
        \[ x_p + y_p := (p,x) + (p,y) = (p,x+y) \]
        \[ \alpha x_p := \alpha (p,x) = (p, \alpha x) \quad \alpha \in \mathbb{R}\]
        In particolare $p_2: T_p \mathbb{R}^n \to \mathbb{R}^n$ $p_2(x_p) = p_2(p,x) = x$ è un isomorfismo di spazi vettoriali 
        \textcolor{violet}{($\Rightarrow$ la dimensione di $T_p \mathbb{R}^n$ è $n$)}.

        $T_p \mathbb{R}^n$ è detto \textcolor{red}{spazio tangente in $p$ a $\mathbb{R}^n$}
        \end{definizione}
        \begin{definizione}
        Sia $A \subset \mathbb{R}^n$ aperto, $F: A \to \mathbb{R}^m$ continua e differenziabile $p \in A$ e 
        
        $\left(dF\right)_p: \mathbb{R}^n \to \mathbb{R}^m$ mediante $F$
        e $\left(dF\right)_p$ si può definire l'applicazione 
        \[ T_p \mathbb{R}^n \to T_{F(p)} \mathbb{R}^m \quad (p,x) \mapsto (F(p), \left(dF\right)_p(x)) \]
        Tale applicazione sarà denotata ancora \textcolor{violet}{(abuso di notazione)} con \textcolor{red}{$\left(dF\right)_p$} e denominata \textcolor{red}{differenziale di $F$ in $p$}.
        
        \textcolor{violet}{In alcune circostanze sarà utile pensare il vettore come vettore applicato in $p$ mandato in un vettore $F(p)$}
        \end{definizione}
        \begin{definizione}
        Sia $A \subset \mathbb{R}^n$ aperto, poniamo 
        \[ T A := \bigcup_{p \in A} T_p \mathbb{R}^n \]
        unione disgiunta degli spazi tangenti a $\mathbb{R}^n$ nei punti di $A$.
        \begin{align*}
        X \in T A &\iff \exists ! \, p \in A \text{ tale che } X \in T_p \mathbb{R}^n=\left\{p\right\} \times \mathbb{R}^n  \\
        &\iff \exists ! \, p \in A \text{ tale che } X=(p,x) \text{ con } x \in \mathbb{R}^n
        \end{align*}
        Si considera l'applicazione 
        $\uppi: T A \to A \quad X \mapsto p $
        
        La terna \textcolor{red}{$(T A, \uppi, A)$} è detta \textcolor{red}{fibrato tangente di $A$} 

        $\uppi$ è detta \textcolor{red}{proiezione del fibrato}

        $TA$ è detto \textcolor{red}{spazio totale del fibrato}

        $A$ è detto \textcolor{red}{spazio base}

        $\forall p \in A \quad \uppi^{-1}(p) = T_p \mathbb{R}^n$ è detta \textcolor{red}{fibra in $p$}
        \end{definizione}
    \section{Curve parametrizzate in \texorpdfstring{$\mathbb{R}^n$}{R\textasciicircum n}}
        \begin{definizione}
        Sia $I \subseteq \mathbb{R}$ un intervallo aperto si definisce \textcolor{red}{curva parametrizzata di $\mathbb{R}^n$} 
        ogni applicazione $c: I \to \mathbb{R}^n$ che sia $C^{\infty}$ differenziabile, \textcolor{red}{$t$} si dice \textcolor{red}{parametro} della curva.

        L'insieme \textcolor{red}{$\left\{c(t) \mid t \in I \right\}$} è detto \textcolor{red}{supporto della curva} e sarà denotato con \textcolor{red}{$\supp(c)$}

        Poniamo $\forall t \in I \quad c(t)= (c^1(t),\ldots,c^n(t))$ con $c^i= p^i \circ c: I \to \mathbb{R}$ per $i=1,\ldots,n$ \textcolor{blu}{(funzioni componenti di $c$)}
        
        $\dot{c}(t)= (\dot{c}^1(t),\ldots,\dot{c}^n(t))$

        Per $t_0 \in I$ si può considerare l'applicazione lineare $\left(dc\right)_{t_0}: \mathbb{R} \to \mathbb{R}^n$ 
        \[ \left(dc\right)_{t_0} \textcolor{blu}{\underbrace{\textcolor{black}{(1)}}_{\substack{\text{base} \\ \text{canonica} \\ \text{di } \mathbb{R}}}} \overset{\textcolor{violet}{\substack{\text{unica} \\ \text{colonna} \\ \text{matrice} \\ \text{Jacobiana}}}}{=} \left(\dot{c}^1(t_0), \ldots, \dot{c}^n(t_0) \right) = \dot{c}(t_0) \] 
        Si definisce inoltre \textcolor{violet}{(per abuso di notazione continuo a denotare come differenziale)}
        \[ \left(dc\right)_{t_0}: T_{t_0} \mathbb{R} \to T_{c(t_0)} \mathbb{R}^n \quad \left(t_0,t\right) \mapsto \left(c(t_0), (dc)_{t_0} (t) \right) \]
        $\left\{(t_0,1)\right\}$ è base di $T_{t_0} \mathbb{R}$ 

        Si definisce \textcolor{red}{vettore tangente in $t_0$ a $c$} il vettore
        \[ \textcolor{red}{\left(dc\right)_{t_0} (t_0,1) = \left( c(t_0), (dc)_{t_0}(1) \right) = \left(c(t_0), \dot{c}(t_0) \right)} \, \textcolor{violet}{\equiv \dot{c}(t_0)} \]
        Il vettore tangente in $t_0$ a $c$ può essere denotato con $\dot{c}(t_0)$ \textcolor{violet}{(abuso di notazione)} omettendo il punto di applicazione $c(t_0)$  
        \end{definizione}
        \begin{definizione}
        Una curva parametrizzata $c: I \to \mathbb{R}^n$ si dice \textcolor{red}{regolare} se \textcolor{red}{$\forall t \in I \quad \dot{c}(t) \neq 0$}
        \textcolor{blu}{(Il vettore tangente in ogni punto della curva è non nullo)} 
        \end{definizione}
        \begin{definizione}
        Sia $c: I \to \mathbb{R}^n$ una curva parametrizzata regolare, si definisce \textcolor{red}{campo di vettori lungo $c$} \textcolor{blu}{(o campo vettoriale lungo $c$)}
        ogni applicazione 
        
        \textcolor{red}{$\hat{X}: I \to T \mathbb{R}^n$ tale che $\forall t \in I \quad \hat{X}(t) \in T_{c(t)} \mathbb{R}^n$} cioè $\hat{X}(t) = \left(c(t), \textcolor{blu}{\underbrace{\textcolor{black}{X(t)}}_{\in \mathbb{R}^n}}\right)$ 

        Resta quindi definita una applicazione $X: I \to \mathbb{R}^n$ e se $X$ è differenziabile si dice che $\hat{X}$ è un \textcolor{red}{campo vettoriale differenziabile lungo $c$} 
        \begin{center}
        \begin{tikzpicture}[scale=1.2]

        % 1. Intervallo I e Mappa c
        \draw[thick] (-3, 0) -- (-1, 0) node[midway, below] {\Large $I$};
        \draw[->, thick, darkgray] (-2, 0.3) to[bend left=25] node[midway, below, text=red, yshift=-2pt] {\Large $c$} (-0.2, 1.4);

        % 2. Curva principale con "ancore" posizionate matematicamente
        % TikZ calcola da solo le coordinate P1, P2, P3, P4 esattamente sulla linea
        \draw[very thick, teal] (0, 1.5) .. controls (1.5, 2.5) and (3, 0) .. (5, 0.5)
            coordinate[pos=0.25] (P1)
            coordinate[pos=0.50] (P2)
            coordinate[pos=0.75] (P3)
            coordinate[pos=0.95] (P4);

        % 3. Campo vettoriale X(t) (vettori variabili)
        % Punto 1: c(t)
        \fill[red] (P1) circle (1.5pt) node[below left, text=red] {\large $c(t)$};
        \draw[-{Stealth[length=3mm]}, very thick, red] (P1) -- ++(1.5, 0.5) node[above, text=red] {\large $X(t)$};

        % Punto 2
        \draw[-{Stealth[length=3mm]}, very thick, red] (P2) -- ++(1.2, 0.1);

        % Punto 3
        \draw[-{Stealth[length=3mm]}, very thick, red] (P3) -- ++(1.0, 0.8);

        % Punto 4
        \draw[-{Stealth[length=3mm]}, very thick, red] (P4) -- ++(0.3, 1.2);

        \end{tikzpicture}
        \end{center}
        Il campo vettoriale può essere anche costante
        \begin{center}
        \begin{tikzpicture}[scale=1.2]

        % 1. Stessa curva principale con le stesse ancore matematiche
        \draw[very thick, teal] (0, 1.5) .. controls (1.5, 2.5) and (3, 0) .. (5, 0.5)
            coordinate[pos=0.25] (P1)
            coordinate[pos=0.50] (P2)
            coordinate[pos=0.75] (P3)
            coordinate[pos=0.95] (P4);

        % 2. Campo vettoriale COSTANTE
        \def\vx{0.9}
        \def\vy{0.5}

        % Punto 1: c(t)
        \fill[red] (P1) circle (1.5pt) node[below left, text=red] {\large $c(t)$};
        \draw[-{Stealth[length=3mm]}, very thick, red] (P1) -- ++(\vx, \vy) node[above, text=red, xshift=10pt] {\large $X(t) \equiv v_0$};

        % Altri punti (frecce identiche per definizione di campo costante)
        \draw[-{Stealth[length=3mm]}, very thick, red] (P2) -- ++(\vx, \vy);
        \draw[-{Stealth[length=3mm]}, very thick, red] (P3) -- ++(\vx, \vy);
        \draw[-{Stealth[length=3mm]}, very thick, red] (P4) -- ++(\vx, \vy);

        \end{tikzpicture}
        \end{center}
        \end{definizione}
        \begin{osservazione}
        Sia $c: I \to \mathbb{R}^n$ una curva parametrizzata regolare consideriamo $\hat{c} : I \to T \mathbb{R}^n$ tale che $\forall t \in I \quad \hat{c}(t) = \left(c(t), \dot{c}(t)\right) \in T_{c(t)} \mathbb{R}^n$
        
        \textcolor{red}{$\hat{c}$} è campo vettoriale differenziabile lungo $c$ detto \textcolor{red}{campo vettoriale tangente} 
        \begin{center}
        \begin{tikzpicture}[scale=1.2,
            % Definisco lo stile "tangente" che automatizza il disegno
            tangente/.style={
                decoration={
                    markings,
                    mark=at position #1 with {
                        % Il puntino rosso sulla curva
                        \fill[red] (0,0) circle (1.5pt);
                        % Il vettore derivato \dot{c}(t) che giace sull'asse x locale (tangente)
                        \draw[-{Stealth[length=3mm]}, very thick, red] (0,0) -- (1.5,0) 
                            node[above left, text=red, yshift=8pt] {\large $\dot{c}(t)$};
                    }
                },
                postaction={decorate}
            }
        ]

        % 1. Intervallo I e Mappa c
        \draw[thick] (-3, 0) -- (-1, 0) node[midway, below] {\Large $I$};
        \draw[->, thick, darkgray] (-2, 0.3) to[bend left=20] 
            node[midway, below, text=teal, yshift=-2pt] {\Large $c$} (-0.2, 1.4);

        % 2. Curva principale con i vettori tangenti applicati automaticamente
        \draw[very thick, teal,
                tangente=0.25,
                tangente=0.55,
                tangente=0.85]
            (0, 1.5) .. controls (1.5, 2.5) and (3, 0) .. (5, 0.5)
            % Aggiungo un'ancora al primo punto per etichettare la base
            coordinate[pos=0.25] (P1);

        % 3. Etichetta per il punto base c(t) (messa solo sul primo per non appesantire il disegno)
        \node[below left, text=red, yshift=2pt] at (P1) {\large $c(t)$};

        \end{tikzpicture}
        \end{center}
        \end{osservazione}
        \newpage
        \begin{definizione}
        Siano $\alpha: I \to \mathbb{R}^n$ e $\beta: J \to \mathbb{R}^n$ curve parametrizzate regolari con $I,J \subset \mathbb{R}$ intervalli aperti

        Si dice che \textcolor{red}{$\alpha$} e \textcolor{red}{$\beta$} sono \textcolor{red}{equivalenti} \textcolor{blu}{(a meno di parametrizzazioni)} 
        se esiste $\phi: I \to J$ diffeomorfismo \textcolor{blu}{(applicazione differenziabile bigettiva con inversa differenziabile)} tale che \textcolor{red}{$\beta = \alpha \circ \phi$}
        \begin{equation*}
        \begin{tikzcd}[row sep=large, column sep=large]
        J \arrow[r, "\phi"] \arrow[rr, bend right, "\beta"'] & I \arrow[r, "\alpha"] & \mathbb{R}^n
        \end{tikzcd}
        \end{equation*}
        Scriveremo che $\beta \sim \alpha$ e $\phi$ è detto cambiamento di parametro
        \end{definizione}
        \begin{osservazione}
        $\phi$ è differenziabile $C^\infty$ e invertibile $\Rightarrow \phi$ è strettamente monotona 
        
        $\Rightarrow \left( \forall s \in J \quad \phi'(s) > 0 \right) \lor \left( \forall s \in J \quad \phi'(s) < 0 \right)$
        \end{osservazione}
        \begin{definizione}
        Si dice che 

        \textcolor{red}{$\phi$ conserva l'orientazione} se $\forall s \in J \quad \phi'(s) > 0$ 

        \textcolor{red}{$\phi$ rovescia l'orientazione} se $\forall s \in J \quad \phi'(s) < 0$
        \end{definizione}
        \begin{osservazione}
        Supponiamo $\alpha \sim \beta$ tramite $\alpha : I \to \mathbb{R}^n$ e $\beta: J \to \mathbb{R}^n$ e $\beta = \alpha \circ \phi$ con $\phi: J \to I$ diffeomorfismo

        $\beta = \alpha \circ \phi \Rightarrow \supp (\alpha) = \supp(\beta)$
        \begin{proof}
        $\supp(\beta)= \beta(J)= \left(\alpha \circ \phi \right)(J) = \alpha(I)= \supp(\alpha)$
        \end{proof}
        $\beta = \alpha \circ \phi \Rightarrow \forall s \in J \quad \dot{\beta}(s) = \phi'(s) \cdot \dot{\alpha}(\phi(s)) $
        \begin{proof}
        \begin{align*}
        \hat{\beta}(s) &= \left( d \beta \right)_s (1) = \left( \alpha \circ \phi \right)_s (1) \overset{\eqref{eq:composizione_di_applicazioni_differenziabili}}{=} \left(d\alpha\right)_{\phi(s)} \circ \left(d\phi\right)_s (1) \\
        &= \left(d\alpha\right)_{\phi(s)} \left( \phi'(s) \right)  = \left(d\alpha\right)_{\phi(s)} \left( \phi'(s) \cdot 1 \right) = \phi'(s) \cdot \left(d\alpha\right)_{\phi(s)} (1)  \\
        &= \phi'(s) \cdot \dot{\alpha}(\phi(s)) 
        \end{align*}
        \end{proof}
        \end{osservazione}
                \begin{center}
        \begin{tikzpicture}[scale=1.3]

        % 1. Disegno la curva principale in alto (colore teal per richiamare il blu/celeste)
        \draw[very thick, teal] (-1, 3) .. controls (2, 4.5) and (5, 1.5) .. (7, 3.5);

        % 2. Disegno i due intervalli J e I in basso (i segmenti neri)
        \draw[thick] (-1.5, 0) -- (0.5, 0);
        \node at (-0.5, -0.4) {\Large $J$};

        \draw[thick] (3.5, 0) -- (5.5, 0);
        \node at (4.5, -0.4) {\Large $I$};

        % 3. Disegno il punto s in J e \phi(s) in I
        \fill[red] (-0.5, 0) circle (1.5pt) node[below left] {$s$};
        \fill[green!70!black] (4.8, 0) circle (1.5pt) node[below right] {$\phi(s)$};

        % 4. Definisco il punto immagine sulla curva \beta(s) = \alpha(\phi(s))
        \coordinate (P) at (1.8, 3.4); 
        \fill[red] (P) circle (1.5pt) node[below left, text=teal, yshift=-3pt, xshift=-12pt] {$\beta(s) = \alpha(\phi(s))$};

        % 5. Frecce delle mappe \phi, \beta, \alpha
        % Mappa \phi (in basso)
        \draw[->, thick, darkgray] (0.2, -0.8) to[bend right=20] node[midway, below, text=teal] {\Large $\phi$} (3.8, -0.8);
        % Mappa \beta (da J alla curva)
        \draw[->, thick, darkgray] (-0.5, 0.3) to[bend left=15] node[midway, above left, text=red] {\large $\beta$} (1.6, 3);
        % Mappa \alpha (da I alla curva)
        \draw[->, thick, darkgray] (4.8, 0.3) to[bend right=20] node[midway, right, text=green!70!black] {\large $\alpha$} (2.1, 3);

        % 6. Vettori tangenti (discordi)
        % Vettore \dot{\alpha} (verde, verso destra)
        \draw[-{Stealth[length=3mm]}, very thick, green!70!black] (P) -- ++(1.5, -0.4) node[above, yshift=2pt] {$\dot{\alpha}(\phi(s))$};
        % Vettore \dot{\beta} (rosso, verso sinistra, discorde perché \phi' < 0)
        \draw[-{Stealth[length=3mm]}, very thick, red] (P) -- ++(-1.1, 0.3) node[above right, xshift=5pt] {$\dot{\beta}(s) \textcolor{black}{=} \textcolor{orange}{\phi'(s)} \textcolor{green!70!black}{\dot{\alpha}(\phi(s))}$};

        % 7. Testo esplicativo arancione in alto a destra
        \node[text=orange, align=left] at (5, 4.5) {se $\phi'(s) < 0$ \\ sono discordi};
        \draw[<-, thick, orange] (4.2, 4.5) to[bend right=15] (3.3, 4);
        \draw[<-, thick, orange] (5, 4) to[bend left=15] (3.1, 3.5);

        \end{tikzpicture}
        \end{center}
        \begin{definizione}
        La relazione di "equivalenza a meno di parametrizzazione" è una relazione di equivalenza (simmetrica , riflessiva e transitiva) sull'insieme
        delle curve parametrizzate regolari.

        Ogni classe di equivalenza \textcolor{red}{$\left[ \alpha \right]_{\sim}$} è detta \textcolor{red}{curva non parametrizzata} 
        \end{definizione}
        \begin{definizione}
        Si dice che $2$ curve parametrizzate regolari $\alpha: I \to \mathbb{R}^n$ e $\beta: J \to \mathbb{R}^n$ sono \textcolor{red}{equivalenti con orientazione} se 
        esiste un cambiamento di parametro $\phi$ che conservi l'orientazione.
        
        Scriveremo \textcolor{red}{$\alpha \sim_0 \beta$} e anche questa è una relazione di equivalenza.
        
        Ogni classe di equivalenza \textcolor{red}{$\left[\alpha\right]_{\sim_0}$} è detta \textcolor{red}{curva non parametrizzata orientata}

        \textcolor{violet}{Vedremo delle proprietà invarianti per cambiamento di parametro}
        \end{definizione}
        \newpage
        \begin{definizione}
        Sia $c: I \to \mathbb{R}^n$ curva parametrizzata regolare, sia $[a,b] \subset I$ e si definisce \textcolor{red}{lunghezza dell'arco di curva $c_{\restriction{[a,b]}}$}
        \begin{equation}
        \label{eq:lunghezza_arco_di_curva}
        L \left(c_{\restriction{[a,b]}}\right)= \int_a^b \norm{\dot{c}(t)} \, dt
        \end{equation}
        \end{definizione}
        \begin{proposizione}
        \label{prop:lunghezza_arco_di_curva_invariante}
        La lunghezza di un arco di curva è invariante per cambiamento di parametro
        \end{proposizione}
        \begin{proof}
        Siano $\alpha:I \to \mathbb{R}^n \quad \beta : J \to \mathbb{R}^n$ curve parametrizzate regolari equivalenti, sia $\phi : J \to I$ diffeomorfismo tale che $\beta = \alpha \circ \phi$

        Sia $[a,b] \subset J$ e verifichiamo che se $\phi$ conserva l'orientazione $\Rightarrow L \left(\beta_{\restriction{[a,b]}}\right)= L\left(\alpha_{\restriction{[\phi(a),\phi(b)]}}\right)$ e
        che se $\phi$ rovescia l'orientazione $\Rightarrow L \left(\beta_{\restriction{[a,b]}}\right)= L\left(\alpha_{\restriction{[\phi(b),\phi(a)]}}\right)$ 
        
        Supponiamo ora che $\phi$ conservi l'orientazione
        \begin{align*}
        L(\beta_{\restriction{[a,b]}}) =& \int_a^b \norm{\dot{\beta}(s)} \, ds = \int_a^b \norm{ \phi'(s) \dot{\alpha}(\phi(s))} \, ds \\
        =& \int_a^b \left|\phi'(s)\right| \, \norm{\dot{\alpha}(\phi(s))} \, ds \overset{\textcolor{blu}{\substack{\text{Supponiamo che} \\ \phi \text{ conservi} \\ \text{l'orientazione}}}}{=} \\
        =& \int_a^b  \phi'(s) \, \norm{\dot{\alpha}(\phi(s))} \, ds \quad \text{ posto } t=\phi(s) \quad dt= \phi'(s) ds \\
        =& \int_{\phi(a)}^{\phi(b)} \norm{\dot{\alpha} (t)} \, dt = L\left(\alpha_{\restriction{[\phi(a),\phi(b)]}}\right)
        \end{align*}
        Supponiamo ora che $\phi$ rovesci l'orientazione
        \begin{align*}
        L(\beta_{\restriction{[a,b]}}) =& \int_a^b \norm{\dot{\beta}(s)} \, ds = \int_a^b \norm{ \phi'(s) \dot{\alpha}(\phi(s))} \, ds \\
        =& \int_a^b \left|\phi'(s)\right| \, \norm{\dot{\alpha}(\phi(s))} \, ds \overset{\textcolor{blu}{\substack{\text{Supponiamo che} \\ \phi \text{ rovesci} \\ \text{l'orientazione}}}}{=} \\
        =& - \int_a^b  \phi'(s) \, \norm{\dot{\alpha}(\phi(s))} \, ds \quad \text{ posto } t=\phi(s) \quad dt= \phi'(s) ds \\
        =& \int_{\phi(b)}^{\phi(a)} \norm{\dot{\alpha} (t)} \, dt = L\left(\alpha_{\restriction{[\phi(b),\phi(a)]}}\right)
        \end{align*}
        \end{proof}
        \begin{definizione}
        Una curva parametrizzata $c: I \to \mathbb{R}^n$ si dice \textcolor{red}{parametrizzata per lunghezza d'arco} se \textcolor{red}{$\forall t \in I \quad \norm{\dot{c}(t)}=1$} 
        \textcolor{blu}{(il vettore tangente in ogni punto è unitario)}
        \end{definizione}
        \begin{osservazione}
        Se $c$ è una curva parametrizzata per lunghezza d'arco $t_0 >0$
        \[ L \left(c_{\restriction{[a,t_0]}}\right) = \int_a^{a+t_0} \norm{\dot{c}(t)} \, dt = a+t_0 - a = t_0 \]
        \end{osservazione}
        \begin{proposizione}
        \label{prop:esistenza_riparametrizzazione_lunghezza_arco}
        Ogni curva parametrizzata regolare ammette una riparametrizzazione per lunghezza d'arco \textcolor{blu}{(equivalente)}
        \end{proposizione}
        \begin{proof}
        Sia $\alpha : I \to \mathbb{R}^n$ curva regolare sia $t_0 \in I$

        Sia \textcolor{red}{$\displaystyle \psi:I \to R \quad \psi(t)=\int_{t_0}^t \norm{\dot{\alpha}(s)} \, ds$} $\psi$ è continua e derivabile
        $\psi'(t) = \norm{\dot{\alpha}(t)} \overset{\textcolor{orange}{\text{regolarità}}}{>} 0 \Rightarrow \psi$ è strettamente crescente $\Rightarrow \psi$ è ingettiva

        $\psi: I \to \psi(I)=J$ è bigettiva e $\phi= \psi^{-1}:J \to I$ è diffeomorfismo

        $\displaystyle \forall s \in J \quad \phi'(s) \overset{\textcolor{violet}{\substack{\text{derivazione} \\ \text{funzione} \\ \text{inversa}}}}{=} 
        \frac{1}{\psi'(\phi(s))} = \frac{1}{\norm{\dot{\alpha}(\phi(s))}}>0$

        Sia \textcolor{red}{$\beta = \alpha \circ \phi:J \to \mathbb{R}^n$} verifico che $\beta$ è parametrizzata per lunghezza d'arco
        \begin{align*}
        \norm{\dot{\beta}(s)} =& \left|\phi'(s)\right| \, \norm{\dot{\alpha} (\phi(s))} \\
        =& \phi'(s) \, \norm{\dot{\alpha} (\phi(s))} = \frac{1}{\norm{\dot{\alpha}(\phi(s))}} \, \norm{\dot{\alpha} (\phi(s))} = 1
        \end{align*}
        \end{proof}
        \newpage
        \begin{esempio}
        Siano $v, v_0 \in \mathbb{R}^n$ e $c: \mathbb{R} \to \mathbb{R}^n \quad \forall t \in \mathbb{R} \quad c(t) = v_0 + t v$
        
        $v_0=\left(v_0^1, \ldots, v_0^n\right) \quad v=\left(v^1, \ldots, v^n\right) \quad c(t)= \left(v_0^1 +tv^1, \ldots, v_0^n +tv^n\right)$

        $\dot{c}(t)=\left(v^1, \ldots, v^n\right)=v$

        $c$ è regolare $\iff v \neq 0$

        Per $v=0$ il supporto di $c$ è $\left\{v_0\right\}$

        Per $v \neq 0$ il supporto di $c$ è la retta passante per $v_0$ di giacitura $<v>$ avente equazioni parametriche:
        \[ \begin{cases*}
        x^1 = v_0^1 + t v^1 \\
        x^2 = v_0^2 + t v^2 \\
        \vdots \\
        x^n = v_0^n + t v^n
        \end{cases*}
        \text{dove } (v^1, \ldots, v^n) \text{ è la } n\text{-upla dei parametri direttori della retta } \]
        \end{esempio}
        \begin{esempio}
        $c_1: \mathbb{R} \to \mathbb{R}^2 \quad c_1(t) =(t,t)=\underbrace{\left(0,0\right)}_{v_0} + t \underbrace{(1,1)}_{v}$
        \begin{center}
        \begin{tikzpicture}[scale=1.2]

        % 1. Disegno gli assi cartesiani (in grigio chiaro per non appesantire)
        \draw[->, thick, gray!80] (-2.5, 0) -- (2.5, 0) node[right, text=black] {$x$};
        \draw[->, thick, gray!80] (0, -2.5) -- (0, 2.5) node[above, text=black] {$y$};

        % 2. Disegno la curva c_1(t) = (t,t), che è la bisettrice del I e III quadrante
        \draw[very thick, teal] (-2.2, -2.2) -- (2.2, 2.2) node[right, xshift=5pt] {$c_1(t)$};

        % 3. Disegno il vettore direzione v = (1,1) in rosso
        \draw[-{Stealth[length=3mm]}, very thick, red] (0,0) -- (1,1) node[midway, above left] {$v=(1,1)$};

        % 4. Evidenzio il punto iniziale v_0 = (0,0)
        \fill[black] (0,0) circle (1.5pt) node[below right, yshift=-2pt] {$v_0 = (0,0)$};

        % 5. Aggiungo un paio di punti sulla curva per far capire il parametro t (opzionale ma didattico)
        \fill[teal] (1,1) circle (1.5pt) node[below right] {$t=1$};
        \fill[teal] (2,2) circle (1.5pt) node[below right] {$t=2$};
        \fill[teal] (-1,-1) circle (1.5pt) node[above left] {$t=-1$};

        \end{tikzpicture}
        \end{center}
        Il supporto di $c_1$ è la retta passante per $v_0=(0,0)$ e di parametri direttori $v=(1,1)$

        $\dot{c_1}(t)=\left(1,1\right) \neq 0 \Rightarrow c_1 \text{ è regolare}$
        \end{esempio}
        \newpage
        \begin{esempio}
        $c_2: \mathbb{R} \to \mathbb{R}^2 \quad c_2(t) =(t, \left|t\right|)$
        \begin{center}
        \begin{tikzpicture}[scale=1.5]

        % 1. Disegno gli assi cartesiani
        \draw[->, thick, gray!80] (-2.5, 0) -- (2.5, 0) node[right, text=black] {$x$};
        \draw[->, thick, gray!80] (0, -1) -- (0, 2.5) node[above, text=black] {$y$};

        % 2. Disegno la curva c_2(t) = (t, |t|) a forma di V
        \draw[very thick, teal] (-2.2, 2.2) -- (0, 0) -- (2.2, 2.2) node[right, xshift=5pt] {$c_2(t)$};

        % 3. Evidenzio il vettore velocità sul ramo sinistro (t < 0)
        \fill[teal] (-1, 1) circle (1.5pt) node[above left, xshift=-12pt] {$t=-1$};
        \draw[-{Stealth[length=3mm]}, very thick, red] (-1, 1) -- (0, 0) node[midway, below left] {$\dot{c}_2(-1) = (1,-1)$};

        % 4. Evidenzio il vettore velocità sul ramo destro (t > 0)
        \fill[teal] (1, 1) circle (1.5pt) node[above left] {$t=1$};
        \draw[-{Stealth[length=3mm]}, very thick, red] (1, 1) -- (2, 2) node[midway, below right] {$\dot{c}_2(1) = (1,1)$};

        % 5. Evidenzio il punto problematico
        \fill[black] (0,0) circle (1.5pt) node[below, yshift=-4pt] {$t=0$ (non differenziabile)};

        \end{tikzpicture}
        \end{center}
        $c_2$ non è derivabile in $0$ e non è $C^{\infty}$
        \end{esempio}
        \begin{esempio}
        $c_3: \mathbb{R} \to \mathbb{R}^2 \quad c_3(t) =(t^3, t^3)$

        Il supporto di $c_3$ coincide con il supporto di $c_1$ tuttavia $c_3$ non è una parametrizzazione regolare
        $\dot{c_3}(t)=\left(3t^2,3t^2\right)$ che si annulla in $t=0$

        \textcolor{violet}{Questo con $c_1$ mostra che due curve possono avere lo stesso supporto ma una può avere una parametrizzazione regolare mentre l'altra no}
        \end{esempio}
        \newpage
        \begin{esempio}
        $c_4: \mathbb{R} \to \mathbb{R}^2 \quad c_4(t) =(t^2, t^3)$
        
        $\dot{c_4}(t)=\left(2t,3t^2\right)$ che si annulla in $t=0 \Rightarrow c_4$ non è regolare
        
        $\supp(c_4)= \left\{\left(x,y\right) \in \mathbb{R}^2 \mid x=t^2 \quad y=t^3\right\} = \left\{(x,y) \in \mathbb{R}^2 \mid \textcolor{blu}{\underbrace{\textcolor{celeste}{x^3 -y^2 = 0}}_{\text{cubica cuspidale}}}\right\}$
        \begin{center}
        \begin{tikzpicture}[scale=1.2]

        % 1. Disegno gli assi cartesiani
        \draw[->, thick, gray!80] (-0.5, 0) -- (3, 0) node[right, text=black] {$x$};
        \draw[->, thick, gray!80] (0, -2.5) -- (0, 2.5) node[above, text=black] {$y$};

        % 2. Disegno la cubica cuspidale c_4(t) = (t^2, t^3)
        % Uso il comando plot parametrico nativo di TikZ
        \draw[very thick, teal, domain=-1.5:1.5, samples=100, variable=\t] 
            plot ({\t*\t}, {\t*\t*\t}) node[right, xshift=5pt] {$c_4(t)$};

        % 3. Evidenzio i vettori tangenti per t=1 e t=-1
        % Per t=1, il punto è (1,1) e il vettore velocità è (2,3)
        \fill[teal] (1, 1) circle (1.5pt) node[right, xshift=2pt] {$t=1$};
        \draw[-{Stealth[length=3mm]}, very thick, red] (1, 1) -- ++(1, 1.5) node[right] {$\dot{c}_4(1) = (2,3)$};

        % Per t=-1, il punto è (1,-1) e il vettore velocità è (-2,3)
        \fill[teal] (1, -1) circle (1.5pt) node[right, xshift=2pt] {$t=-1$};
        \draw[-{Stealth[length=3mm]}, very thick, red] (1, -1) -- ++(-1, 1.5) node[left] {$\dot{c}_4(-1) = (-2,3)$};

        % 4. Evidenzio l'origine dove si perde la regolarità
        \fill[black] (0,0) circle (1.5pt) node[below left, yshift=-2pt] {$t=0$ (non regolare)};

        \end{tikzpicture}
        \end{center}
        \end{esempio}
        \newpage
        \begin{esempio}
        Sia $I \subset \mathbb{R}$ intervallo aperto e $f: I \to \mathbb{R}$ funzione derivabile di classe $C^\infty$ definiamo 

        $c:I \to \mathbb{R}^2 \quad c(t) = (t, f(t))$

        $\dot{c}(t) = (1, f'(t)) \neq (0,0) \Rightarrow c$ è regolare

        Il supporto di $c$ è il grafico di $f \Rightarrow \supp(c) = \graf(f)$
        
        $\dot{c}(t) = (1, f'(t)) \neq (0,0) \quad \norm{\dot{c}(t)} = \sqrt{1 + (f'(t))^2}=1$

        $c$ è parametrizzata per lunghezza d'arco \textcolor{violet}{$\iff \norm{\dot{c}(t)}=1$} $\iff f'(t)=0 \quad \forall t \in I \iff f$ costante
        \textcolor{violet}{Quindi questo tipo di parametrizzazione non è mai per lunghezza d'arco se $f$ non è costante}

        $\ddot{c}(t) = (0, f''(t))$ 

        Se $f''(t) >0$ il grafico è concavo verso l'alto

        Se $f''(t) <0$ il grafico è concavo verso il basso
        \begin{center}
        \begin{tikzpicture}[scale=1.4]

        % 1. Disegno gli assi cartesiani (spessi neri come nel tuo schizzo)
        \draw[->, very thick, black] (-3, 0) -- (4, 0);
        \draw[->, very thick, black] (0, -1.5) -- (0, 3.5);

        % 2. Disegno la curva f(t) (in teal)
        % Uso i comandi 'to[out=..., in=...]' per forzare la curva a passare 
        % esattamente per i nostri punti con la giusta pendenza
        \draw[very thick, teal] 
            (-2.5, -0.6) to[out=45, in=195] 
            (-1.5, 0.2) to[out=15, in=210] 
            (0.5, 0.6) to[out=30, in=225] 
            (1.2, 1.2) to[out=45, in=240] 
            (2.2, 3);

        % 3. Punto a sinistra: concavità verso il basso (f''(t) < 0)
        \coordinate (PL) at (-1.5, 0.2);
        \fill[green!70!black] (PL) circle (1.5pt);
        \draw[-{Stealth[length=3mm]}, very thick, green!70!black] (PL) -- ++(0, -1.2) node[right, yshift=4pt] {\Large $\ddot{c}(t)$};

        % 4. Punto intermedio: concavità verso l'alto (f''(t) > 0)
        \coordinate (PG) at (0.5, 0.6);
        \fill[green!70!black] (PG) circle (1.5pt);
        \draw[-{Stealth[length=3mm]}, very thick, green!70!black] (PG) -- ++(0, 1.4) node[above] {\Large $\ddot{c}(t)$};

        % 5. Punto a destra: Vettore tangente e scomposizione
        \coordinate (PR) at (1.2, 1.2);
        \fill[red] (PR) circle (1.5pt) node[below=4pt, text=red] {\large $c(t)$};
        
        % Derivata prima \dot{c}(t)
        \draw[-{Stealth[length=3mm]}, very thick, red] (PR) -- ++(1.4, 1.4) node[midway, above left, xshift=2pt] {\Large $\dot{c}(t)$};
        
        % Scomposizione del vettore in componenti orizzontale e verticale (triangolo rettangolo)
        \draw[thick, darkgray] (PR) -- ++(1.4, 0) node[midway, below, text=red] {\Large $1$};
        \draw[thick, darkgray] (2.6, 1.2) -- ++(0, 1.4) node[midway, right, text=red] {\Large $f'(t)$};

        \end{tikzpicture}
        \end{center}
        \end{esempio}
        \newpage
        \begin{esempio}
        \textcolor{violet}{è possibile che $2$ curve che abbiano lo stesso supporto e che siano regolari non siano equivalenti?
        non sempre ci vorranno condizioni aggiuntive ecco un controesempio}

        $\alpha: [0,2\pi] \to \mathbb{R}^2 \quad \alpha(t) = (\cos t, \sin t) \quad \beta: [0,2\pi] \to \mathbb{R}^2 \quad \beta(t) = (\cos (2t), \sin (2t))$

        $\dot{\alpha}(t) = (-\sin t, \cos t) \quad \norm{\dot{\alpha}(t)} = 1$

        $\dot{\beta}(t) = (-2\sin (2t), 2\cos (2t)) \quad \norm{\dot{\beta}(t)} = 2$
        
        $\alpha$ e $\beta$ sono curve parametrizzate regolari
        \[ L(\alpha) = \int_0^{2\pi} \norm{\dot{\alpha}(t)} \, dt = \int_0^{2\pi} 1 \, dt = 2\pi \]
        \[ L(\beta) = \int_0^{2\pi} \norm{\dot{\beta}(t)} \, dt = \int_0^{2\pi} 2 \, dt = 4\pi \]
        $\overset{\textcolor{black}{ \text{prop. } \ref{prop:lunghezza_arco_di_curva_invariante}}}{\Rightarrow} \alpha$ e $\beta$ non sono equivalenti \textcolor{blu}{(se lo fossero gli archi di curva considerati avrebbero la stessa lunghezza)}
        \end{esempio}
        \newpage
        \begin{esempio}
        $c: \mathbb{R} \to \mathbb{R}^3 \quad c(t)=(a \cos t, a \sin t, bt) \quad a,b \in \mathbb{R} \quad a,b \geq 0$ 
        
        $\dot{c}(t) = (-a \sin t, a \cos t, b)$ 

        $\norm{\dot{c}(t)} = \sqrt{a^2 \sin^2 t + a^2 \cos^2 t + b^2} = \sqrt{a^2 + b^2} = 0 \iff (a,b) = (0,0)$
        \begin{enumerate}
            \item[I] CASO $a=0 \quad b \neq 0 \quad c(t) = (0,0,bt)$ 
            \[ \begin{cases*}
                x(t) = 0 \\
                y(t) = 0 \\
                z(t) = bt
            \end{cases*} \quad \text{cioè la parametrizzazione dell'asse} \quad z:\begin{cases*}
                x = 0 \\
                y = 0 
            \end{cases*} \]
            

            \item[II] CASO $a \neq 0 \quad b = 0 \quad c(t) = (a \cos t, a \sin t, 0)$
            \[ \begin{cases*}
                x(t) = a \cos t \\
                y(t) = a \sin t \\
                z(t) = 0
            \end{cases*} \] 
            cioè la parametrizzazione della circonferenza nel piano $xy$ con centro $(0,0,0)$ e raggio $a$:
            \[ \begin{cases*}
                x^2 + y^2 = a^2 \\
                z = 0
            \end{cases*} \]
            \begin{center}
            \begin{tikzpicture}[scale=1.2]

            % 1. Assi (finta prospettiva 3D)
            \draw[->, thick, black] (0,0) -- (-1.5, -1.5) node[right, font=\large] {$x$};
            \draw[->, thick, black] (0,0) -- (3, 0) node[below, font=\large] {$y$};
            \draw[->, thick, black] (0,0) -- (0, 2.5) node[right, font=\large] {$z$};

            % Origine
            \node[above left] at (0,0) {$O$};

            % 2. Circonferenza nel piano xy (resa come ellisse per la prospettiva)
            \draw[very thick, teal] (0,0) ellipse (2cm and 0.6cm);

            % 3. Intersezione con l'asse y indicata con "a"
            \fill[red] (2,0) circle (1.5pt) node[below right, text=red] {\large $a$};

            % 4. Vettore velocità iniziale sulla curva
            % Geometricamente a t=0 la curva si trova in (a, 0, 0), che nel nostro 
            % disegno corrisponde al punto più basso dell'ellisse.
            \coordinate (P) at (-0.58, -0.6);
            \draw[-{Stealth[length=3mm]}, very thick, red] (P) -- ++(1.5, 0) node[below, text=red, yshift=-2pt, xshift=-2pt] {\large $\dot{c}(0) = (0,a,0)$};

            % 5. Testi e formule in alto a destra
            \node[text=teal, right, align=left] at (2.5, 1) {\large $\dot{c}(t) = (-a\sin t, a\cos t, 0)$};

            % 6. Nota in ambra (usando il colore "amber" che hai definito nel preambolo)
            \draw[->, thick, amber] (2.8, -0.6) to[bend left=15] (3.3, -0.6);
            \node[text=amber, right] at (3.3, -0.6) {\large Stiamo considerando $a>0$};

            \end{tikzpicture}
            \end{center}
            \item[III] CASO $a \neq 0 \quad b \neq 0 \quad c(t) = (a \cos t, a \sin t, bt)$
            \[ \begin{cases*}
                x(t) = a \cos t \\
                y(t) = a \sin t \\
                z(t) = bt
            \end{cases*} \]
            $\supp(c)$ è contenuto nel cilindro di equazione $x^2 + y^2 = a^2$, si osservi inoltre che 
            $c(t+2k \pi)= (a \cos t, a \sin t, b(t+2k \pi))$ cioè $c$ è una parametrizzazione dell'elica cilindrica di passo $2\pi b$.
            \begin{center}
            % Modifico temporaneamente gli angoli visivi per questo specifico disegno 3D
            \tdplotsetmaincoords{70}{110} 
            \begin{tikzpicture}[tdplot_main_coords, scale=1.5]

            % 1. Assi cartesiani in 3D
            \draw[->, thick, black] (-1.5, 0, 0) -- (2, 0, 0) node[right] {$x$};
            \draw[->, thick, black] (0, -1.5, 0) -- (0, 2, 0) node[left] {$y$};
            \draw[->, thick, black] (0, 0, 0) -- (0, 0, 3.5) node[above] {$z$};

            % 2. Circonferenza di base del cilindro (proiezione della curva sul piano xy)
            % Uso la variabile \t per tracciare il cerchio raggio a=1
            \draw[dashed, darkgray, domain=0:2*pi, samples=100, variable=\t] 
                plot ({cos(\t r)}, {sin(\t r)}, 0);

            % 3. Elica cilindrica
            % La lettera "r" (es. \t r) serve a dire a TikZ di calcolare cos e sin in radianti
            \draw[very thick, teal, domain=0:5*pi, samples=200, variable=\t] 
                plot ({cos(\t r)}, {sin(\t r)}, {0.2*\t}) node[right, yshift=-16pt] {$c(t)$};

            % 4. Punto di partenza sull'asse x
            \fill[red] (1, 0, 0) circle (1.5pt) node[below right, text=red] {$a$};

            % 5. Evidenziazione del passo (quota dopo 1 giro completo)
            % A t=2\pi, la curva si trova in (1, 0, 0.2*2\pi)
            %\draw[thick, dashed, amber] (1, 0, {0.2*2*pi}) -- (1.5, 0, {0.2*2*pi});
            %\draw[thick, dashed, amber] (1, 0, 0) -- (1.5, 0, 0);
            \draw[-, thick, amber] (1.3, 0, 0) -- (1.3, 0, {0.2*2*pi}) node[midway, right, text=amber, xshift=-40pt, yshift=10pt] {\large $2\pi b$};

            \end{tikzpicture}
            \end{center}
            Verifichiamo che l'angolo $\theta(t)$ che $\dot{c}(t)$ forma con l'asse $z$ è costante
            $\dot{c}(t) = (-a \sin t, a \cos t, b)$ $\displaystyle \cos \theta(t) = \frac{\dot{c}(t) \cdot e_3}{\norm{\dot{c}(t)} \cdot \norm{e_3}} = \frac{b}{\sqrt{a^2 + b^2}}$

            Cerchiamo una parametrizzazione della curva per lunghezza d'arco \textcolor{violet}{(come nella dimostrazione della proposizione \ref{prop:esistenza_riparametrizzazione_lunghezza_arco})} 
            
            consideriamo $\psi: \mathbb{R} \to \mathbb{R}$ definita da 
            $\displaystyle \psi(t) = \int_0^t \norm{\dot{c}(r)} \, dr = \int_0^t \sqrt{a^2 + b^2} \, dr = t \sqrt{a^2 + b^2}$

            $\displaystyle \phi := \psi^{-1} : \mathbb{R} \to \mathbb{R} \quad \phi(s) = \frac{s}{\sqrt{a^2 + b^2}}$.

            $\beta = c \circ \phi : \mathbb{R} \to \mathbb{R}^3$
            $\displaystyle \beta(s) = c(\phi(s)) = \left( a \cos\left(\frac{s}{\sqrt{a^2 + b^2}}\right), a\sin\left(\frac{s}{\sqrt{a^2 + b^2}}\right), b \frac{s}{\sqrt{a^2 + b^2}} \right)$

            $\displaystyle \norm{\dot{\beta}(s)} = \textcolor{violet}{ \norm{\dot{c}(\phi(s)) \cdot \phi'(s)} = \norm{\dot{c}(\phi(s))} \cdot \abs{\phi'(s)} = \frac{\sqrt{a^2 + b^2}}{\sqrt{a^2 + b^2}}} = 1$
        \end{enumerate}
        \end{esempio}
    \section{Curve differenziabili}
        \begin{definizione}
        Sia $\mathcal{C} \subseteq \mathbb{R}^n$ e si consideri su $\mathcal{C}$ la topologia indotta dalla topologia euclidea
        \textcolor{blu}{($U_{\mathcal{C}} \subseteq \mathcal{C}$ è aperto $\iff U_{\mathcal{C}} = U \cap \mathcal{C}$ con $U \subseteq \mathbb{R}^n$ aperto)}
        
        Si dice che \textcolor{red}{$\mathcal{C}$} è una \textcolor{red}{curva differenziabile} se \textcolor{red}{$\forall p \in \mathcal{C}$ esiste $\alpha : I_{\alpha} \to U_{\alpha} \subset \mathcal{C} \subset \mathbb{R}^n$ 
        omeomorfismo} tra $I_{\alpha}$ intervallo aperto di $\mathbb{R}$ e $U_{\alpha}$ aperto di $\mathcal{C}$ tale che $p \in U_{\alpha}$ questa
        $\alpha$ è una curva parametrizzata regolare e $\alpha$ è detta \textcolor{red}{parametrizzazione locale} della curva.

        Si osservi che il supporto della parametrizzazione $\alpha$ è $\alpha(I_{\alpha})=U_{\alpha}$

        Posto \textcolor{red}{$\varphi_{\alpha} := \alpha^{-1}$} $: U_{\alpha} \subset \mathcal{C} \to I_{\alpha} \subset \mathbb{R}$ la coppia 
        \textcolor{red}{$\left(U_{\alpha}, \varphi_{\alpha}\right)$} è detta \textcolor{red}{carta locale} nel punto $p$ dalla definizione segue che una curva differenziabile 
        ammette una famiglia di carte locali \textcolor{red}{$\{(U_{\alpha}, \varphi_{\alpha})\}_{\alpha \in A}$} tale che $\displaystyle \mathcal{C}=\bigcup_{\alpha \in A} U_{\alpha}$
        tale famiglia è detta \textcolor{red}{atlante differenziabile} 

        \textcolor{violet}{Si può osservare che $\mathcal{C}$ è uno spazio topologico localmente euclideo perché ogni punto $p \in \mathcal{C}$ ammette
        un intorno aperto $U_{\alpha}$ omeomorfo a un intervallo aperto di $\mathbb{R} \Rightarrow \dim \mathcal{C}=1$}
        \begin{center}
        \begin{tikzpicture}[scale=1.2]

        % Il trucco: definisco l'equazione della curva in una variabile
        \def\curva{3.5 + 0.4*sin(\x * 45)}

        % 1. Curva principale C in R^2 (dominio totale)
        \draw[very thick, teal, domain=-3.5:4.5, samples=100, variable=\x] 
            plot ({\x}, {\curva}) node[right, font=\Large] {$\mathcal{C} \subset \mathbb{R}^2$};

        % 2. Intorni U_alpha, U_beta e intersezione (stessa funzione, domini ridotti)
        % U_alpha (arancione semitrasparente)
        \draw[line width=6pt, orange, opacity=0.4, domain=-1.5:1.2, samples=50, variable=\x] 
            plot ({\x}, {\curva});
        \node[orange, above] at (-0.9, 3.4) {\large $U_\alpha$};

        % U_beta (verde semitrasparente)
        \draw[line width=6pt, green!70!black, opacity=0.4, domain=-0.2:2.8, samples=50, variable=\x] 
            plot ({\x}, {\curva});
        \node[green!70!black, above] at (2.2, 3.9) {\large $U_\beta$};

        % Intersezione U_alpha \cap U_beta (rosso netto)
        \draw[line width=2pt, red, domain=-0.2:1.2, samples=30, variable=\x] 
            plot ({\x}, {\curva});
        \node[red, above] at (0.5, 3.8) {\large $U_\alpha \cap U_\beta$};

        % Punto p (piazzato matematicamente sulla curva a x=0.5)
        \coordinate (P) at (0.5, {3.5 + 0.4*sin(0.5 * 45)});
        \fill[red] (P) circle (1.5pt) node[below=3pt] {\large $p$};

        % 3. Domini delle carte I_alpha e I_beta
        \draw[thick, orange] (-2.5, 0) -- (-0.5, 0) node[midway, below] {\large $I_\alpha$};
        \draw[thick, green!70!black] (1.5, -1) -- (3.5, -1) node[midway, below] {\large $I_\beta$};

        % 4. Frecce per la carta alpha
        \draw[->, thick, orange] (-1.5, 0.3) to[bend right=15] node[midway, right] {\large $\alpha$} (-0.8, 3.1);
        \draw[->, thick, darkgray] (-1.3, 3.0) to[bend right=15] node[midway, left] {\large $\varphi_\alpha$} (-2.0, 0.3);

        % 5. Frecce per la carta beta
        \draw[->, thick, green!70!black] (2.5, -0.7) to[bend left=15] node[midway, left] {\large $\beta$} (1.8, 3.3);
        \draw[->, thick, darkgray] (2.3, 3.2) to[bend left=15] node[midway, right] {\large $\varphi_\beta$} (3.0, -0.7);

        % 6. Funzione di transizione phi
        \draw[->, thick, red] (-0.8, -0.7) to[bend right=22] (1.8, -1.6);
        \node[red, below] at (0.5, -1.6) {\large $\phi = \beta^{-1} \circ \alpha = \varphi_\beta \circ \varphi_\alpha^{-1}$};

        \end{tikzpicture}
        \end{center}
        \begin{equation}
        \alpha^{-1} \left(U_{\alpha} \cap U_{\beta} \right) \subset I_\alpha \xrightarrow[]{\quad \alpha \quad} U_{\alpha} \cap U_{\beta} \xrightarrow[]{\quad \beta^{-1} \quad} \beta^{-1} \left(U_{\alpha} \cap U_{\beta} \right) \subset I_\beta
        \end{equation}
        \end{definizione}
        \newpage
        \begin{proposizione}
        \label{prop:funzione_di_transizione_curve}
        Sia $\mathcal{C} \subset \mathbb{R}^n$ curva differenziabile siano $\alpha: I_\alpha \to U_\alpha$ e $\beta: I_\beta \to U_\beta$ due parametrizzazioni 
        locali di $\mathcal{C}$ 
        
        Se $U_\alpha \cap U_\beta \neq \emptyset$, allora l'applicazione 
        
        $\textcolor{red}{\beta^{-1} \circ \alpha}: \alpha^{-1} \left(U_\alpha \cap U_\beta \right) \to \beta^{-1} \left(U_\alpha \cap U_\beta \right)$ 
        \textcolor{blu}{$\left(\equiv  \varphi_\beta \circ \varphi_\alpha^{-1} : \varphi_{\alpha} \left(U_\alpha \cap U_\beta \right) \to \varphi_\beta \left(U_\alpha \cap U_\beta \right) \right)$} è 
        
        un \textcolor{red}{diffeomorfismo}
        \end{proposizione}
        \begin{proof}
        Sia $t_0 \in \alpha^{-1} \left(U_{\alpha} \cap U_{\beta} \right) \subset I_\alpha$
        
        Verifichiamo che $\beta^{-1} \circ \alpha$ è differenziabile in un intorno di $t_0$

        Posto \textcolor{amber}{$p = \alpha(t_0)$} $\in U_{\alpha} \cap U_{\beta}$ sia \textcolor{amber}{$s_0= \beta^{-1}(p)$} $\in \beta^{-1} \left(U_{\alpha} \cap U_{\beta} \right) \subset I_\beta$
        $\Rightarrow \alpha(t_0)= \beta(s_0)=p$

        Poniamo $\forall s \in I_\beta \quad \beta(s)=\left(x^1(s), \ldots, x^n(s)\right)$ ovvero $x^i(s)=p^i(\beta(s))$ 
        con $p^i: \mathbb{R}^n \to \mathbb{R}$ proiezione

        $\dot{\beta}(s) = \left(\dot{x}^1(s), \ldots, \dot{x}^n(s)\right)$
        
        \textcolor{orange}{$\beta$ è regolare} $\Rightarrow \dot{\beta}(s_0) \neq 0 \Rightarrow \exists i \in \{1, \ldots, n\}$ tale che $\dot{x}^i(s_0) \neq 0$

        La funzione $s \in \beta^{-1} \left(U_{\alpha} \cap U_{\beta} \right) \subset I_\beta \mapsto x^i(s)= p^i(\beta(s)) \in \mathbb{R}$ 
        è differenziabile.

        Per il teorema della funzione inversa $\exists J $ intorno aperto di $s_0 \quad J \subset \beta^{-1} \left(U_{\alpha} \cap U_{\beta} \right)$ ed
        $\exists V$ intorno aperto di $x^i(s_0) = p^i(\beta(s_0)) = p^i(p)$ tale che \textcolor{celeste}{$p^i \circ \beta: J \to V$ sia un diffeomorfismo}.

        $\textcolor{violet}{\underbrace{\textcolor{verde}{(p^i)^{-1}(V)}}_{\text{aperto in } \mathbb{R}^n}} \cap \mathcal{C}$ è un aperto di $\mathcal{C}$ che contiene $\beta(s_0)=\alpha(t_0)=p$.

        \textcolor{orange}{Per la continuità di $\alpha$} esiste un intervallo aperto $I \subset \alpha^{-1} \left(U_\alpha \cap U_\beta \right)$ tale che
        $t_0 \in I$
        
        $\alpha(I) \subset (p^i)^{-1}(V) \cap \mathcal{C}$ \textcolor{violet}{(Quindi $\left(p^i \circ \alpha \right)(I) \subset V$ )}
        \[ I \xrightarrow[]{\quad p^i \circ \alpha \quad} V \xrightarrow[]{\quad \left( \textcolor{celeste}{p^i \circ \beta} \right)^{-1} \quad} J \]
        Questa composizione è differenziabile 

        \textcolor{violet}{Non possiamo scrivere $\left(p^i \circ \beta \right)^{-1} = \beta^{-1} \circ (p^i)^{-1}$ perché $(p^i)^{-1}$ non è 
        invertibile valuto però} 
        
        \textcolor{violet}{$(p^i \circ \beta)(\beta^{-1}(\alpha(t)))= (p^i \circ \alpha)(t)$ e $\beta$ è invertibile in $I$ essendo un omeomorfismo.}

        $\forall t \in I \quad \left(p^i \circ \beta \right)^{-1} \left( \left( p^i \circ \alpha \right) (t) \right) = \beta^{-1} \left( \alpha (t) \right)$
        \textcolor{blu}{(perché $\left(p^i \circ \beta \right) \left( \beta^{-1}(\alpha(t)) \right)) = \left(p^i \circ \alpha \right) (t)$)}

        Pertanto $\beta^{-1} \circ \alpha : I \to J$ è differenziabile.

        Si potrebbe ripetere la dimostrazione scambiando i ruoli di $\alpha$ e $\beta$ risulta che $\alpha^{-1} \circ \beta$ è differenziabile.

        Pertanto $\beta^{-1} \circ \alpha : \alpha^{-1} \left(U_{\alpha} \cap U_{\beta} \right) \to \beta^{-1} \left(U_{\alpha} \cap U_{\beta} \right)$ è diffeomorfismo.
        \end{proof}
        \begin{osservazione}
        Posto $\phi = \beta^{-1} \circ \alpha \quad \alpha = \beta \circ \phi$ con $\phi$ diffeomorfismo quindi le $2$ curve parametrizzate sono equivalenti. 
        \end{osservazione}
        \newpage
        \begin{esempio}
        Sia $c:I \to \mathbb{R}^n$ curva parametrizzata regolare tale che $c:I \to c(I)$ è omeomorfismo.
        Allora $\mathcal{C}=c(I)$ è una curva differenziabile e un atlante differenziabile è dato da $\{(\mathcal{C}, c^{-1})\}$ con
        \[ c^{-1}: \mathcal{C}= c(I) \to I \]
        \end{esempio}
        \begin{esempio}
        Sia $f : I \to \mathbb{R}$ applicazione $C^\infty$ differenziabile allora il grafico di $f$ è una curva differenziabile in $\mathbb{R}^2$.

        Infatti $c:I \to \mathbb{R}^2$ definita da $c(t) = (t, f(t))$ è una curva parametrizzata regolare.

        Posto \textcolor{red}{$\mathcal{C}=c(I)=\graf(f)$} $c:I \to \mathcal{C}$ è un omeomorfismo, essendo $t \mapsto \left(t, f(t)\right)$ bigettiva e continua.

        e $c^{-1}: \mathcal{C} \to I \quad \left(t,f(t)\right) \mapsto t$ è continua.
        \end{esempio}
        \begin{esempio}
        $\mathcal{C}= \left\{(x,y) \in \mathbb{R}^2 \mid x^2 +y^2 =1 \right\}$ $\mathcal{C}$ è la circonferenza di centro $(0,0)$ e raggio $1$.

        $\mathcal{C}$ non è grafico di funzione verifichiamo che $\mathcal{C}$ è curva differenziabile.
        
        Poniamo $V_1 = \left\{(x,y) \in \mathbb{R}^2 \mid y > 0 \right\}$ e $V_2 = \left\{(x,y) \in \mathbb{R}^2 \mid y < 0 \right\}$ aperti di 
        $\mathbb{R}^2$. 

        $U_1 = \mathcal{C} \cap V_1$ e $U_2 = \mathcal{C} \cap V_2$ sono aperti di $\mathcal{C}$.

        $U_1$ è la semicirconferenza senza gli estremi $c_1: (-1,1) \to \mathbb{R}^2$ definita da $c_1(t) = (t, \sqrt{1-t^2})$
        $c_1(-1,1)=U_1$ è il grafico della funzione $f(t)=\sqrt{1-t^2} \quad c_1: (-1,1) \to U_1$ è omeomorfismo 
        e parametrizzazione regolare di $U_1$.

        $U_2$ è la semicirconferenza senza gli estremi $c_2: (-1,1) \to \mathbb{R}^2$ definita da $c_2(t) = (t, -\sqrt{1-t^2})$
        $c_2(-1,1)=U_2$ è il grafico della funzione $f(t)=-\sqrt{1-t^2} \quad c_2: (-1,1) \to U_2$ è omeomorfismo 
        e parametrizzazione regolare di $U_2$.
        \begin{center}
        \begin{tikzpicture}[scale=1.7]

        % ==========================================
        % GRAFICO SINISTRO: U_1 e c_1
        % ==========================================
        \begin{scope}[xshift=-2cm]
            % Aperto V_1 (y > 0) evidenziato in grigio chiarissimo
            \fill[gray!10] (-1.5, 0) rectangle (1.5, 1.5);
            \node[gray, right] at (-1.4, 1.3) {$V_1 = \{y > 0\}$};

            % Assi cartesiani
            \draw[->, thick, gray!80] (-1.6, 0) -- (1.6, 0) node[right, text=black] {$x$};
            \draw[->, thick, gray!80] (0, -1.5) -- (0, 1.6) node[above, text=black] {$y$};

            % Circonferenza completa C (tratteggiata)
            \draw[dashed, darkgray] (0,0) circle (1);

            % Supporto di U_1 (semicirconferenza superiore)
            \draw[very thick, teal, domain=0:180, samples=100] plot ({cos(\x)}, {sin(\x)});
            \node[teal, above right] at (0.707, 0.707) {\Large $U_1$};

            % Estremi esclusi (-1,0) e (1,0)
            \filldraw[fill=white, draw=teal, thick] (1,0) circle (1.5pt) node[below right, text=black] {$1$};
            \filldraw[fill=white, draw=teal, thick] (-1,0) circle (1.5pt) node[below left, text=black] {$-1$};

            % Intervallo di parametrizzazione (-1,1) sull'asse x
            \draw[line width=4pt, orange, opacity=0.5] (-1,0) -- (1,0);
            \node[orange, below=2pt] at (-0.5, 0) {\small $t \in (-1,1)$};

            % Mappatura del punto t -> c_1(t)
            \coordinate (T1) at (0.4, 0);
            \coordinate (P1) at (0.4, {sqrt(1 - 0.4^2)});
            \fill[red] (T1) circle (1.5pt) node[below=2pt, text=black] {$t$};
            \fill[red] (P1) circle (1.5pt) node[above right, text=red, yshift=2pt, xshift=-6pt] {$c_1(t)$};
            \draw[-, dashed, red, thick] (T1) -- (P1) node[midway, left] {$f(t)$};
        \end{scope}

        % ==========================================
        % GRAFICO DESTRO: U_2 e c_2
        % ==========================================
        \begin{scope}[xshift=2cm]
            % Aperto V_2 (y < 0) evidenziato in grigio chiarissimo
            \fill[gray!10] (-1.5, -1.5) rectangle (1.5, 0);
            \node[gray, right] at (-1.4, -1.3) {$V_2 = \{y < 0\}$};

            % Assi cartesiani
            \draw[->, thick, gray!80] (-1.6, 0) -- (1.6, 0) node[right, text=black] {$x$};
            \draw[->, thick, gray!80] (0, -1.5) -- (0, 1.6) node[above, text=black] {$y$};

            % Circonferenza completa C (tratteggiata)
            \draw[dashed, darkgray] (0,0) circle (1);

            % Supporto di U_2 (semicirconferenza inferiore)
            \draw[very thick, teal, domain=180:360, samples=100] plot ({cos(\x)}, {sin(\x)});
            \node[teal, below right] at (0.707, -0.707) {\Large $U_2$};

            % Estremi esclusi (-1,0) e (1,0)
            \filldraw[fill=white, draw=teal, thick] (1,0) circle (1.5pt) node[above right, text=black] {$1$};
            \filldraw[fill=white, draw=teal, thick] (-1,0) circle (1.5pt) node[above left, text=black] {$-1$};

            % Intervallo di parametrizzazione (-1,1) sull'asse x
            \draw[line width=4pt, orange, opacity=0.5] (-1,0) -- (1,0);
            \node[orange, above=2pt] at (-0.5, 0) {\small $t \in (-1,1)$};

            % Mappatura del punto t -> c_2(t)
            \coordinate (T2) at (0.4, 0);
            \coordinate (P2) at (0.4, {-sqrt(1 - 0.4^2)});
            \fill[red] (T2) circle (1.5pt) node[above=2pt, text=black] {$t$};
            \fill[red] (P2) circle (1.5pt) node[below right, text=red, yshift=-2pt, xshift=-6pt] {$c_2(t)$};
            \draw[-, dashed, red, thick] (T2) -- (P2) node[midway, left] {$f(t)$};
        \end{scope}
        \end{tikzpicture}
        \end{center}
        $V_3= \left\{ (x,y) \in \mathbb{R}^2 \mid x>0 \right\} \quad U_3 = V_3 \cap \mathcal{C}$ aperto di $\mathcal{C}$
        $c_3: (-1,1) \to \mathbb{R}^2 \quad c_3(t)=\left(\sqrt{1-t^2}, t\right)$ 
        $c_3: (-1,1) \to U_3$ è omeomorfismo e parametrizzazione regolare 

        $V_4= \left\{ (x,y) \in \mathbb{R}^2 \mid x<0 \right\} \quad U_4 = V_4 \cap \mathcal{C}$ aperto di $\mathcal{C}$
        $c_4: (-1,1) \to \mathbb{R}^2 \quad c_4(t)=\left(-\sqrt{1-t^2}, t\right)$ 
        $c_4: (-1,1) \to U_4$ è omeomorfismo e parametrizzazione regolare 
        \begin{center}
        \begin{tikzpicture}[scale=1.7]

        % ==========================================
        % GRAFICO SINISTRO: U_3 e c_3
        % ==========================================
        \begin{scope}[xshift=-2cm]
            % Aperto V_3 (x > 0) evidenziato in grigio chiarissimo
            \fill[gray!10] (0, -1.5) rectangle (1.5, 1.5);
            \node[gray, right] at (0.1, 1.3) {$V_3 = \{x > 0\}$};

            % Assi cartesiani
            \draw[->, thick, gray!80] (-1.6, 0) -- (1.6, 0) node[right, text=black] {$x$};
            \draw[->, thick, gray!80] (0, -1.6) -- (0, 1.6) node[above, text=black] {$y$};

            % Circonferenza completa C (tratteggiata)
            \draw[dashed, darkgray] (0,0) circle (1);

            % Supporto di U_3 (semicirconferenza destra)
            \draw[very thick, teal, domain=-90:90, samples=100] plot ({cos(\x)}, {sin(\x)});
            \node[teal, above right] at (0.707, 0.707) {\Large $U_3$};

            % Estremi esclusi (0,1) e (0,-1)
            \filldraw[fill=white, draw=teal, thick] (0,1) circle (1.5pt) node[above left, text=black] {$1$};
            \filldraw[fill=white, draw=teal, thick] (0,-1) circle (1.5pt) node[below left, text=black] {$-1$};

            % Intervallo di parametrizzazione (-1,1) sull'asse y
            \draw[line width=4pt, orange, opacity=0.5] (0,-1) -- (0,1);
            \node[orange, left=2pt] at (0.1, -0.1) {\small $t \in (-1,1)$};

            % Mappatura del punto t -> c_3(t)
            \coordinate (T3) at (0, 0.4);
            \coordinate (P3) at ({sqrt(1 - 0.4^2)}, 0.4);
            \fill[red] (T3) circle (1.5pt) node[left=2pt, text=black] {$t$};
            \fill[red] (P3) circle (1.5pt) node[above right, text=red] {$c_3(t)$};
            \draw[-, dashed, red, thick] (T3) -- (P3) node[midway, below] {$f(t)$};
        \end{scope}

        % ==========================================
        % GRAFICO DESTRO: U_4 e c_4
        % ==========================================
        \begin{scope}[xshift=2cm]
            % Aperto V_4 (x < 0) evidenziato in grigio chiarissimo
            \fill[gray!10] (-1.5, -1.5) rectangle (0, 1.5);
            \node[gray, left] at (-0.1, 1.3) {$V_4 = \{x < 0\}$};

            % Assi cartesiani
            \draw[->, thick, gray!80] (-1.6, 0) -- (1.6, 0) node[right, text=black] {$x$};
            \draw[->, thick, gray!80] (0, -1.6) -- (0, 1.6) node[above, text=black] {$y$};

            % Circonferenza completa C (tratteggiata)
            \draw[dashed, darkgray] (0,0) circle (1);

            % Supporto di U_4 (semicirconferenza sinistra)
            \draw[very thick, teal, domain=90:270, samples=100] plot ({cos(\x)}, {sin(\x)});
            \node[teal, above left] at (-0.707, 0.707) {\Large $U_4$};

            % Estremi esclusi (0,1) e (0,-1)
            \filldraw[fill=white, draw=teal, thick] (0,1) circle (1.5pt) node[above right, text=black] {$1$};
            \filldraw[fill=white, draw=teal, thick] (0,-1) circle (1.5pt) node[below right, text=black] {$-1$};

            % Intervallo di parametrizzazione (-1,1) sull'asse y
            \draw[line width=4pt, orange, opacity=0.5] (0,-1) -- (0,1);
            \node[orange, right=2pt] at (-0.04, -0.1) {\small $t \in (-1,1)$};

            % Mappatura del punto t -> c_4(t)
            \coordinate (T4) at (0, 0.4);
            \coordinate (P4) at ({-sqrt(1 - 0.4^2)}, 0.4);
            \fill[red] (T4) circle (1.5pt) node[right=2pt, text=black] {$t$};
            \fill[red] (P4) circle (1.5pt) node[above left, text=red] {$c_4(t)$};
            \draw[-, dashed, red, thick] (T4) -- (P4) node[midway, below] {$f(t)$};
        \end{scope}
        \end{tikzpicture}
        \end{center}
        Posto $\varphi_i = c_i^{-1}: U_i \to (-1,1)$ per $i=1,2,3,4$ e $\displaystyle \left\{\left(U_i, \varphi_i\right)\right\}_{i=1,2,3,4}$ è un
        atlante differenziabile su $\mathcal{C}$ per la proposizione \ref{prop:funzione_di_transizione_curve} segue che per ogni 
        $i,j \in \{1,2,3,4\}$ con $i \neq j$ tale che $U_i \cap U_j \neq \emptyset$
        $\varphi_i \circ \varphi_j^{-1}= c_i^{-1} \circ c_j: \varphi_j(U_i \cap U_j) \to \varphi_i(U_i \cap U_j)$ è un diffeomorfismo.

        \textcolor{violet}{Tramite la proiezione stereografica si potrà trovare un atlante differenziabile con $2$ carte.}
        \end{esempio}
    \section{Riferimenti di Frenet}
        \begin{definizione}
        Sia $c: I \to \mathbb{R}^n$ una curva parametrizzata si dice \textcolor{red}{$n$-riferimento mobile lungo $c$} una famiglia di applicazioni
        differenziabili \textcolor{red}{$e_i: I \to \mathbb{R}^n$} per $i=1,\dots,n$ tali che 
        
        $\forall t \in I \quad \forall i,j \in \{1,\dots,n\} \quad$ \textcolor{red}{$e_i(t) \cdot e_j(t) = \delta_{ij}$}
        \textcolor{blu}{(ovvero $\forall t \in I \quad (e_i(t))_{i=1,\dots,n}$ è base ortonormale di $\mathbb{R}^n$)}
        Identificando $\mathbb{R}^n \equiv T_{c(t)}\mathbb{R}^n \quad \left(e_i(t)\right)_{1 \leq i \leq n}$ può 
        essere riguardata come una famiglia di campi vettoriali differenziabili lungo $c$ \textcolor{blu}{($e_i(t) \equiv (c(t), e_i(t)) \in T_{c(t)}\mathbb{R}^n$)}.
        \begin{center}
        \begin{tikzpicture}[scale=1.5]

        % 1. Intervallo I
        \draw[thick] (0, 0) -- (1.5, 0);
        \draw[thick] (0, -0.15) -- (0, 0.15);
        \draw[thick] (1.5, -0.15) -- (1.5, 0.15);
        \node[below=4pt] at (0.75, 0) {\Large $I$};

        % 2. Curva parametrizzata in R^2 (supporto)
        \draw[very thick, teal] (-0.5, 1.2)
            to[out=80, in=170] (1.2, 2)
            to[out=-10, in=190] (3.5, 1.5)
            to[out=10, in=200] (5.5, 2.5)
            node[right, text=black] {\Large $\mathbb{R}^2$};

        % 3. Mappa c dall'intervallo alla curva
        \draw[->, thick, teal] (0.8, 0.3) to[bend left=35] node[midway, left] {\large $c$} (1, 1.6);

        % 4. Primo punto c(t) e i suoi vettori ortonormali
        \coordinate (P1) at (1.2, 2);
        \fill[green!70!black] (P1) circle (1.5pt) node[below=3pt] {$c(t_1)$};
        
        % Vettore e1(t) (verso l'alto a sinistra)
        \draw[-{Stealth[length=3mm]}, very thick, green!70!black] (P1) -- ++(-0.4, 0.8) node[above left=-2pt] {$e_1(t_1)$};
        % Vettore e2(t) (verso l'alto a destra)
        \draw[-{Stealth[length=3mm]}, very thick, green!70!black] (P1) -- ++(0.8, 0.4) node[above right=-2pt] {$e_2(t_1)$};

        % 5. Secondo punto c(t') e i suoi vettori ortonormali
        \coordinate (P2) at (3.5, 1.5);
        \fill[green!70!black] (P2) circle (1.5pt) node[below=3pt] {$c(t_2)$};
        
        % Vettore e1(t_2) (verso l'alto)
        \draw[-{Stealth[length=3mm]}, very thick, green!70!black] (P2) -- ++(0.2, 0.8) node[above right=-2pt] {$e_1(t_2)$};
        % Vettore e2(t_2) (verso destra)
        \draw[-{Stealth[length=3mm]}, very thick, green!70!black] (P2) -- ++(0.8, -0.2) node[below right=-2pt] {$e_2(t_2)$};

        \end{tikzpicture}
        \end{center}
        \end{definizione}
        \begin{definizione}
        Un $n$-riferimento mobile lungo $c$ $\left(e_i\right)_{1 \leq i \leq n}$ si dice \textcolor{red}{riferimento di Frenet}
        se 
        
        $\forall k \leq n-1 \quad \forall t \in I \quad c^{(k)}(t) \in \langle e_1(t), \dots, e_k(t)\rangle$ \textcolor{blu}{($c^{(k)}$ denota il vettore
        derivato di $c$ $k$-volte)}
        \end{definizione}
        \begin{esempio}
        $c_I \to \mathbb{R}^2$ $(e_1, e_2)$ riferimento mobile lungo $c$ 

        $(e_1, e_2)$ è un riferimento di Frenet lungo $c \iff \forall t \in I \quad c'(t) \in \langle e_1(t) \rangle$
        
        \textcolor{violet}{In altre parole, il primo vettore del riferimento di Frenet è sempre tangente alla curva.}
        \begin{center}
        \begin{tikzpicture}[scale=1.5,
            % Questo stile crea un "timbro" che disegna in automatico i vettori 
            % e_1 (tangente) ed e_2 (normale) nei punti specificati della curva.
            frenet/.style={
                decoration={
                    markings,
                    mark=at position #1 with {
                        % Il vettore rosso e_1 si allinea automaticamente alla tangente
                        \draw[-{Stealth[length=3mm]}, very thick, red] (0,0) -- (1,0) node[below right, yshift=-2pt] {\large $e_1$};
                        % Il vettore verde e_2 viene ruotato ortogonalmente
                        \draw[-{Stealth[length=3mm]}, very thick, green!70!black] (0,0) -- (0,-1.2) node[right] {\large $e_2$};
                    }
                },
                postaction={decorate}
            }
        ]

        % Disegno la curva e applico il timbro "frenet" al 20%, 55% e 88% della lunghezza
        \draw[very thick, teal,
                frenet=0.20,
                frenet=0.55,
                frenet=0.88]
            (0, 2) to[out=30, in=160] (2.5, 1.8) to[out=-20, in=170] (5, 0.5) to[out=-10, in=200] (7.5, 1.5);

        \end{tikzpicture}
        \end{center}
        \end{esempio}
        \newpage
        \begin{teorema} [esistenza e unicità del riferimento canonico di Frenet]
        \label{th:esistenza_e_unicita_del_riferimento_canonico_di_Frenet}
        Sia $c: I \to \mathbb{R}^n$ una curva parametrizzata tale che 
        \begin{equation}
        \label{eq:ipotesi_teorema_di_esistenza_e_unicita_del_riferimento_canonico_di_Frenet}
        \forall t \in I \quad \text{i vettori} \quad \dot{c}(t), \, \ddot{c}(t), \, \dots, \, c^{(n-1)}(t) \quad \text{sono linearmente indipendenti}
        \end{equation}
        Allora esiste un unico riferimento di Frenet $(e_i)_{1 \leq i \leq n}$ tale che 
        \begin{enumerate}
            \item $\forall k \in \{1, \dots, n-1\} \quad \forall t \in I \quad \langle e_1(t), \dots, e_k(t) \rangle = \langle \dot{c}(t), \dots, c^{(k)}(t) \rangle$ e
            le basi $\{ e_1(t), \dots, e_k(t) \}$ e $\{ \dot{c}(t), \dots, c^{(k)}(t) \}$ sono concordemente orientate.
            \item $\forall t \in I \quad \{ e_1(t), \dots, e_n(t) \} \quad \text{è una base ortonormale di } \mathbb{R}^n$ concordemente 
            orientata con la base canonica di $\mathbb{R}^n$.
        \end{enumerate}
        \end{teorema}
        \begin{proof}
        Per costruire i campi vettoriali $e_1(t), \dots, e_{n-1}(t)$ si procede mediante ortogonalizzazione di Gram-Schmidt ai vettori  $\dot{c}(t), \ddot{c}(t), \dots, c^{(n-1)}(t)$.
        
        Poniamo \textcolor{red}{$\displaystyle e_1(t) = \frac{\dot{c}(t)}{\|\dot{c}(t)\|}$}

        Fisso $1 \leq j \leq n-1$ e suppongo di aver costruito i vettori $e_1(t), \dots, e_{j-1}(t)$ tali che 
        
        $\langle \dot{c}(t), \dots, c^{(j-1)}(t) \rangle = \langle e_1(t), \dots, e_{j-1}(t) \rangle$ con basi concordemente orientate.
        
        Devo costruire $e_j(t)$

        Pongo $\displaystyle \tilde{e}_j(t) = c^{(j)}(t) - \sum_{r=1}^{j-1} \langle c^{(r)}(t), e_r(t) \rangle e_r(t)$
        
        Osservo che $\tilde{e}_j(t) \neq 0$ perché se \textcolor{red}{per assurdo} fosse $\tilde{e}_j(t) = 0$ allora $\displaystyle c^{(j)}(t)= \sum_{r=1}^{j-1} \langle c^{(r)}(t), e_r(t) \rangle e_r(t)$
        $\Rightarrow c^{(j)}(t) \in \langle e_1(t), \dots, e_{j-1}(t) \rangle=\langle \dot{c}(t), \dots, c^{(j-1)}(t) \rangle$ contro l'ipotesi \eqref{eq:ipotesi_teorema_di_esistenza_e_unicita_del_riferimento_canonico_di_Frenet}.\label{eq:ip} 
        
        Quindi posso definire $\displaystyle e_j(t) = \frac{\tilde{e}_j(t)}{\|\tilde{e}_j(t)\|}$

        Per costruzione $\forall r,s \in \{1, \dots, j\} \quad  e_r(t) \cdot e_s(t) = \delta_{rs}$ e 
        $\langle \dot{c}(t), \dots, c^{(j)}(t) \rangle = \langle e_1(t), \dots, e_j(t) \rangle$ con $1 \leq j \leq n-1$.

        Verifichiamo che le basi $\langle \dot{c}(t), \dots, c^{(j)}(t) \rangle$ e $\langle e_1(t), \dots, e_j(t) \rangle$ siano effettivamente concordemente orientate.
        
        $\displaystyle \dot{c}(t) = \norm{\dot{c}(t)} e_1(t)$

        $\displaystyle c^{(j)}(t)=\norm{\tilde{e}_j(t)} e_j(t) + \sum_{r=1}^{j-1} \left( c^{(r)}(t) \cdot e_r(t) \right) e_r(t)$ 
        
        la matrice di passaggio da $\langle e_1(t), \dots, e_j(t) \rangle$ a $\langle \dot{c}(t), \dots, c^{(j)}(t) \rangle$ è:

        \[ A(t) =
        \begin{pmatrix}
        \norm{\dot{c}(t)} & * & \cdots & * \\
        0 & \norm{\tilde{e}_2(t)} & \cdots & * \\
        \vdots & \vdots & \ddots & \vdots \\
        0 & 0 & \cdots & \norm{\tilde{e}_j(t)}
        \end{pmatrix}
        \]
        $A(t)$ è una matrice triangolare superiore $\Rightarrow \det(A(t)) \textcolor{violet}{= \norm{\dot{c}(t)} \norm{\tilde{e}_2(t)} \cdots \norm{\tilde{e}_j(t)}} > 0 \Rightarrow$ 
        le basi sono concordamente orientate.

        Si osservi che per costruzione i campi vettoriali $e_1(t), \dots, e_{n-1}(t)$ sono differenziabili \textcolor{violet}{(perchè sono ottenuti da combinazioni lineari differenziabili dei vettori $c^{(1)}(t), \dots, c^{(n-1)}(t)$)}

        Devo costruire $e_n(t)=(x_1(t), \dots, x_n(t))$ 

        $\forall j \in \{1, \dots, n-1\} \quad e_j(t) = \left(e_j^1(t), \, \dots, \, e_j^n(t) \right)$

        $\forall j \in \{1, \dots, n-1\} \quad e_j(t) \cdot e_n(t) = 0 \Rightarrow e_j^1(t) x_1(t) + \cdots + e_j^n(t) x_n(t) = 0$

        $(x_1(t), \dots, x_n(t))$ è soluzione del sistema lineare omogeneo di $n-1$ equazioni in $n$ incognite
        \[ C(t)X(t)= 0 \]
        \[ C(t) = 
        \begin{pmatrix}
        e_1^1(t) & \cdots & e_1^n(t) \\
        \vdots & \ddots & \vdots \\
        e_{n-1}^1(t) & \cdots & e_{n-1}^n(t)
        \end{pmatrix}
        \]
        $\rg(C(t)) = n-1$ poichè i vettori $e_1(t), \dots, e_{n-1}(t)$ sono linearmente indipendenti le $\infty^1$ soluzioni tutte 
        proporzionali alla $n$-upla dei minori di ordine $n-1$ della matrice $C(t)$ presi a segni alterni ovvero
        \[ \left(x_1(t), \, \dots, \, x_n(t) \right) = \rho \left( M_1(t), \, -M_2(t), \, \dots, \, (-1)^{n-1} M_n(t) \right) \quad \textcolor{violet}{\rho \in \mathbb{R}} \]
        \textcolor{violet}{dove $M_i(t)$ è il minore di ordine $n-1$ della matrice $C(t)$ ottenuto eliminando la $i$-esima colonna.}
        \begin{align*}
        \rho \left[ e_j^1(t) M_1 - e_j^2(t) M_2 + \cdots + (-1)^{n-1} e_j^n(t) M_n \right] 
        &= \rho \begin{vmatrix}
        e_1^1(t) & \cdots & e_1^n(t) \\
        \vdots & \ddots & \vdots \\
        e_{n-1}^1(t) & \cdots & e_{n-1}^n(t) \\
        e_j^1(t) & \cdots & e_j^n(t)
        \end{vmatrix} \\[1ex]
        &= \rho \begin{vmatrix}
        &  &  \\
        & C(t) &  \\
        &  &  \\
        e_j^1(t) & \cdots & e_j^n(t)
        \end{vmatrix} \overset{\textcolor{violet}{\substack{\text{2 righe} \\ \text{uguali}}}}{=} 0
        \end{align*}
        $e_n(t) = \left(x_1(t), \dots, x_n(t) \right) \quad e_n(t) \cdot e_n(t) = 1 \Rightarrow x_1^2(t) + \cdots + x_n^2(t) = 1$

        $\displaystyle \Rightarrow \rho(t)^2 \left[ M_1^2(t) + \cdots + M_n^2(t) \right] = 1 \Rightarrow \rho(t) = \pm \dfrac{1}{\quad \sqrt{M_1^2(t) + \cdots + M_n^2(t)} \quad}$

        Si sceglie il segno $+$ o il segno $-$ in modo tale che $\left(e_1(t), \, \dots, \, e_n(t) \right)$ sia concorde con la
        base ortonormale di $\mathbb{R}^n$ infine per costruzione $e_n(t)$ è differenziabile.

        Verifico l'unicità del riferimento canonico di Frenet siano $\left(e_i\right)_{1 \leq i \leq n}$ e $\left(\overline{e}_i\right)_{1 \leq i \leq n}$ due riferimenti di Frenet 
        che soddisfano le condizioni \textcolor{black}{1.} e \textcolor{black}{2.}

        Proviamo che $\forall i = 1, \dots, n \quad e_i = \overline{e}_i$

        Per $i=1$ $\langle e_1(t) \rangle = \langle \overline{e}_1(t) \rangle = \langle \dot{c}(t) \rangle$ con $\left(e_1(t)\right)$ e $\left(\overline{e}_1(t)\right)$ basi ortonormali
        concordemente orientate $\displaystyle \Rightarrow e_1(t) = \overline{e}_1(t) = \frac{\dot{c}(t)}{\|\dot{c}(t)\|}$

        Per $1<j\leq n-1$ supponiamo \textcolor{orange}{$e_k(t) = \overline{e}_k(t) \quad \forall k = 1, \dots, j-1$} e proviamo che $e_j(t) = \overline{e}_j(t)$

        $\left(e_1(t), \, \dots , \, e_j(t)\right) \quad \left(\overline{e}_1(t), \, \dots , \, \overline{e}_j(t)\right)$ sono basi ortonormali concordemente orientate dello stesso
        sottospazio vettoriale \textcolor{violet}{ di dimensione $j$} la matrice di passaggio è 
        \[ A = \begin{pmatrix}
        1 & 0 & \cdots & 0 \\
        0 & 1 & \cdots & 0 \\
        \vdots & \vdots & \ddots & \vdots \\
        0 & 0 & \cdots & \pm 1
        \end{pmatrix} \]
        Poiché le basi sono concordemente orientate, il determinante della matrice di passaggio deve essere $+1$, quindi l'ultimo elemento deve essere $+1$ e quindi $e_j(t) = \overline{e}_j(t)$.
        \end{proof}
        \begin{definizione}
        Una curva parametrizzata che soddisfa l'ipotesi \eqref{eq:ipotesi_teorema_di_esistenza_e_unicita_del_riferimento_canonico_di_Frenet} si dice \textcolor{red}{
        fortemente regolare}
        \end{definizione}
        \begin{osservazione}
        Fortemente regolare $\Rightarrow$ regolare
        \end{osservazione}
        \begin{proof}[Non dimostrato]
        \mbox{}

        \textcolor{violet}{Sia $c(t)$ una curva fortemente regolare $\iff \dot{c}(t)$ è lin. indip. $\iff c(t) \neq 0 \iff c(t)$ è regolare.}
        \end{proof}
        \begin{definizione}
        Il riferimento univocamente determinato dal teorema \ref{th:esistenza_e_unicita_del_riferimento_canonico_di_Frenet} si chiama \textcolor{red}{riferimento canonico di Frenet}.
        \end{definizione}
        \newpage
        \begin{esempio}[Riferimento canonico di Frenet per una curva di $\mathbb{R}^2$]
        \mbox{}

        Sia $c:I \to \mathbb{R}^2$ curva parametrizzata fortemente regolare, determiniamo il riferimento canonico di Frenet.

        Il riferimento canonico di Frenet $(e_1(t), e_2(t))$ è dato da
        \begin{enumerate}
            \item $\displaystyle e_1(t) = \frac{\dot{c}(t)}{\|\dot{c}(t)\|}$
            \item $e_2(t)$ versore tale che $(e_1(t), e_2(t))$ sia una base ortonormale di $\mathbb{R}^2$ concordemente orientata con la base canonica di $\mathbb{R}^2$
        \end{enumerate}
        In generale se $e_1(t)=\left(\alpha(t), \beta(t)\right) \Rightarrow e_2(t) = \left(-\beta(t), \alpha(t)\right)$ infatti
        \[ \norm{e_2(t)} = \sqrt{(-\beta(t))^2 + (\alpha(t))^2} = \sqrt{\beta(t)^2 + \alpha(t)^2} = \norm{e_1(t)} =1 \]
        \[ e_2(t) \cdot e_1(t) = (-\beta(t), \alpha(t)) \cdot (\alpha(t), \beta(t)) = -\beta(t)\alpha(t) + \alpha(t)\beta(t) = 0 \]
        La matrice di passaggio dalla base canonica di $\mathbb{R}^2$ alla base $(e_1(t), e_2(t))$ è
        \[ A(t) = \begin{pmatrix}
        \alpha(t) & -\beta(t) \\
        \beta(t) & \alpha(t)
        \end{pmatrix} \in O(2) \]
        Inoltre, poiché $\det(A(t)) = \alpha(t)\alpha(t) - (-\beta(t))\beta(t) = \alpha(t)^2 + \beta(t)^2 = 1 > 0 \Rightarrow A(t) \in SO(2)$
        \end{esempio}
        \begin{esempio}
        Sia $c: \mathbb{R} \to \mathbb{R}^2 \quad c(t) = (a \cos t, a \sin t) \quad a>0$ $c$ è la parametrizzazione della circonferenza di raggio $a$ centrata nell'origine.

        $\dot{c}(t) = (-a \sin t, a \cos t) \quad \norm{\dot{c}(t)} = \sqrt{(-a \sin t)^2 + (a \cos t)^2}  = a$

        $\displaystyle e_1(t) = \frac{\dot{c}(t)}{\|\dot{c}(t)\|} = \frac{1}{a}(-a \sin t, a \cos t) = (-\sin t, \cos t)$

        $e_2(t)= (\,-\cos t, -\sin t\,)$

        Considero $\phi : \mathbb{R} \to \mathbb{R} \quad s \mapsto -s$ diffeomorfismo 
        $\displaystyle \tilde{c} = c \circ \phi: \mathbb{R} \to \mathbb{R}$ è un'altra parametrizzazione della circonferenza
        
        $\displaystyle \tilde{c}(s) = \left(-a \sin s, -a \cos s\right) \quad \displaystyle \norm{ \, \dot{\tilde{c}}(s) \,} = \sqrt{ \, (-a \cos s)^2 + (a \sin s)^2 \, } = a$
        
        $\displaystyle \tilde{e}_1(s) = \frac{\dot{\tilde{c}}(s)}{\norm{\dot{\tilde{c}}(s)}} = \left(- \sin s , - \cos s \right)$

        $\tilde{e}_2(s) = (\cos s, -\sin s)$
        \end{esempio}
        \newpage
        \begin{definizione}
        \mbox{}

        Sia $c: I \to \mathbb{R}^n$ curva parametrizzata sia $\left(e_i\right)_{1 \leq i \leq n}$ $n$-riferimento mobile lungo $c$

        Poniamo $\forall t \in I \quad \forall i =1, \dots, n \quad$ \textcolor{red}{$\displaystyle \dot{e}_i(t)=\sum_{j=1}^{n} \omega_{ij}(t) e_j(t)$}

        dove $\forall i,j = 1, \dots, n \quad \omega_{ij}(t) = \dot{e}_i(t) \cdot e_j(t)$

        Verifichiamo che $\forall t \in I \quad \forall i,j = 1, \dots, n \quad \omega_{ij}(t) = -\omega_{ji}(t)$ da cui segue che la matrice 
        
        $\omega(t) = \left( \omega_{ij}(t) \right)_{1 \leq i,j \leq n}$ è una matrice antisimmetrica.
        \begin{proof}
        $e_i(t) \cdot e_j(t) = \delta_{ij}$ differenziando si ha 

        $\dot{e}_i(t) \cdot e_j(t) + e_i(t) \cdot \dot{e}_j(t) = 0 \quad \forall i,j = 1, \dots, n$
        da cui segue che $\omega_{ij}(t) + \omega_{ji}(t) = 0$ 
        
        $\Rightarrow \omega_{ij}(t) = -\omega_{ji}(t)$
        \end{proof}
        Le $n$ equazioni 
        \begin{equation}
        \label{eq:equazioni_di_Frenet}
        \dot{e}_i(t) = \sum_{j=1}^{n} \omega_{ij}(t) e_j(t), \quad i = 1, \dots, n   
        \end{equation}
        si chiamano \textcolor{red}{equazioni di Frenet di $c$ nel riferimento mobile $\left(e_i\right)_{1 \leq i \leq n}$}

        poniamo 
        \[ e(t) = \begin{pmatrix} \textcolor{blu}{\overbrace{\textcolor{black}{e_1(t)}}^{\substack{\text{prima riga} \\ \text{di } \omega(t)}}} \\ \vdots \\ e_n(t) \end{pmatrix} \in M_{n} (\mathbb{R}) 
        \qquad \dot{e}_1(t) = \begin{pmatrix} \dot{e}_1(t) \\ \vdots \\ \dot{e}_n(t) \end{pmatrix} \]
        Le equazioni di Frenet \eqref{eq:equazioni_di_Frenet} possono essere riscritte in forma matriciale come
        \begin{equation}
        \label{eq:equazioni_di_Frenet_matriciale}
        \dot{e}(t) = \omega(t) e(t)
        \end{equation}
        \end{definizione}
        \begin{osservazione}
        Sia $\phi :J \to I$ un diffeomorfismo \textcolor{violet}{(cambiamento di parametro)} e poniamo 

        $\tilde{c} = c \circ \phi :J \to I$  \textcolor{violet}{(riparametrizzazione di $c$)}

        Consideriamo \textcolor{red}{$\left(\tilde{e}_i\right)_{1 \leq i \leq n} \quad \tilde{e}_i = e_i \circ \phi : J \to \mathbb{R}^n$} $\quad \tilde{e}_i(s) = e_i(\phi(s)) \quad \forall s \in J$

        $\left(\tilde{e}_i\right)_{1 \leq i \leq n}$ è un $n$-riferimento mobile lungo $\tilde{c}$ e consideriamo le corrispondenti equazioni di Frenet

        \[ \dot{\tilde{e}}_i(s) = \sum_{j=1}^{n} \tilde{\omega}_{ij}(s) \tilde{e}_j(s), \quad i = 1, \dots, n \]
        Sappiamo che $\dot{\tilde{c}}(s) = \dot{c}(\phi(s)) \phi'(s)$ osserviamo che se $\dot{\tilde{c}}(s) \neq 0$ allora 
        \[ \qquad \textcolor{red}{\frac{\tilde{\omega}_{ij}(s)}{\norm{\dot{\tilde{c}}(s)}} = \pm \frac{\omega_{ij}(\phi(s))}{\norm{\dot{c}(\phi(s))}}} \qquad \left(+ \text{ se } \phi'(s) > 0 \text{ e } - \text{ se } \phi'(s) < 0 \right) \]
        \begin{proof}
        \begin{align*}
        \frac{\tilde{\omega}_{ij}(s)}{\norm{\dot{\tilde{c}}(s)}} &= \frac{ \dot{\tilde{e_i}}(s) \cdot \tilde{e}_j(s) }{ \, \left|\phi ' (s)\right| \, \norm{\dot{c}(\phi(s))} \, } = \frac{ \phi'(s) \dot{e_i}(\phi(s)) \cdot e_j(\phi(s)) }{ \, \left|\phi ' (s)\right| \, \norm{\dot{c}(\phi(s))} \, } \\
        &= \textcolor{blu}{\underbrace{ \textcolor{verde}{\frac{\phi'(s)}{|\phi'(s)|}}}_{\pm 1}} \, \frac{\omega_{ij}(\phi(s))}{ \, \norm{\dot{c}(\phi(s))} \,}
        \end{align*}
        \end{proof}
        \end{osservazione}
        \begin{proposizione}
        Sia $c: I \to \mathbb{R}^n$ curva fortemente regolare e sia $\left(e_i\right)_{1 \leq i \leq n}$ il riferimento canonico di Frenet allora
        \textcolor{red}{$\omega_{ij} (t) =0 \quad \forall j > i+1$} ovvero
        \[ \omega(t) = 
        \begin{pmatrix}
        0 & \omega_{12}(t) & 0 & \cdots & 0 \\
        -\omega_{12}(t) & 0 & \omega_{23}(t) & \cdots & 0 \\
        0 & -\omega_{23}(t) & 0 & \cdots & 0 \\
        \vdots & \vdots & \vdots & \ddots & \vdots \\
        0 & 0 & 0 & \cdots & 0
        \end{pmatrix} \]
        \end{proposizione}
        \begin{proof}
        \mbox{}

        $\forall i = 1, \dots, n-1 \quad e_i(t) \in \langle e_1(t), \dots, e_i(t) \rangle = \langle \dot{c}(t), \ddot{c}(t), \dots, c^{(i)}(t) \rangle$

        $\Rightarrow e_i(t)$ è combinazione lineare dei vettori $\dot{c}(t), \ddot{c}(t), \dots, c^{(i)}(t)$
        \[ e_i(t) = \sum_{r=1}^{i} b_i^r(t) c^{(r)}(t) \]
        \[ \dot{e}_i(t) = \sum_{r=1}^{i} \left( \dot{b}_i^r(t) c^{(r)}(t) + b_i^r(t) c^{(r+1)}(t) \right) \]
        $\Rightarrow \dot{e}_i(t)$ è combinazione lineare dei vettori $\dot{c}(t), \ddot{c}(t), \dots, c^{(i+1)}(t)$
        
        $\Rightarrow \dot{e}_i(t) \in \langle \dot{c}(t), \ddot{c}(t), \dots, c^{(i+1)}(t) \rangle = \langle e_1(t), e_2(t), \dots, e_{i+1}(t) \rangle $

        Pertanto $\forall j > i+1 \quad e_j(t) \in \langle e_1(t), \dots, e_{i+1}(t) \rangle^\perp \quad \omega_{ij}(t) = \dot{e}_i(t) \cdot e_j(t) = 0$
        \end{proof}
        \newpage
        \begin{definizione}
        Sia $c : I \to \mathbb{R}^n$ curva parametrizzata fortemente regolare sia $\left(e_i\right)_{1 \leq i \leq n}$ il riferimento canonico di Frenet.

        Si definiscono \textcolor{red}{funzioni di curvatura} di $c$ le $n-1$ funzioni differenziabili date da 

        \[ \textcolor{red}{k_i : I \to \mathbb{R} \quad i = 1, \dots, n-1 \quad \forall t \in I \quad k_i(t) = \frac{\omega_{i,i+1}(t)}{\norm{\dot{c}(t)}} = \frac{\dot{e_i} (t) \cdot e_{i+1}(t)}{\norm{\dot{c}(t)}}} \]
        \end{definizione}
        \begin{proposizione}
        \mbox{}
        
        Per $n \geq 3$ le prime $n-2$ funzioni di curvatura sono \textcolor{red}{positive}, ovvero 

        \textcolor{red}{$\forall i = 1, \dots, n-2 \quad k_i(t) >0 \quad \forall t \in I$} \textcolor{blu}{($\iff \omega_{i,i+1}(t) > 0 \quad \forall t \in I$)}
        \[ \begin{pmatrix}
            0 & \textcolor{celeste}{\omega_{12}(t)} & \cdots & \cdots & 0 \\
            -\omega_{12}(t) & 0 & \cdots & \cdots & 0 \\
            \vdots & \vdots & 0 & \textcolor{celeste}{\omega_{n-2,n-1}(t)} & 0 \\
            \vdots & \cdots & -\omega_{n-2,n-1}(t) & 0 & \textcolor{celeste}{\omega_{n-1,n}(t)} \\
            0 & \cdots & \cdots & -\omega_{n-1,n}(t) & 0
            \end{pmatrix} \quad \textcolor{celeste}{\omega_{i,i+1}(t)> 0} \]
        \end{proposizione}
        \begin{proof}
        \mbox{}

        Bisogna ricordare la costruzione del riferimento canonico di Frenet in particolare 
        
        $\forall j \leq n-1 \quad \langle \dot{c}(t), \ddot{c}(t), \dots, c^{(j)}(t) \rangle = \langle e_1(t), \dots, e_j(t) \rangle$
        e la matrice di passaggio da $\left(e_1(t), \, \dots, \, e_j(t)\right)$ a $ \left(\dot{c}(t), \ddot{c}(t), \dots, c^{(j)}(t)\right)$ è:
        \[ A(t) =
        \begin{pmatrix}
        \norm{\dot{c}(t)} & * & \cdots & * \\
        0 & \norm{\tilde{e}_2(t)} & \cdots & * \\
        \vdots & \vdots & \ddots & \vdots \\
        0 & 0 & \cdots & \norm{\tilde{e}_j(t)}
        \end{pmatrix}
        \]
        \[ c^{(j)}(t) = \sum_{r=1}^{j} a_j^r (t) e_r(t) \quad a_j^j(t) = \norm{\tilde{e}_j(t)}> 0 \]
        La matrice di passaggio da $ \left(\dot{c}(t), \ddot{c}(t), \dots, c^{(j)}(t)\right)$ a $\left(e_1(t), \, \dots, \, e_j(t)\right)$ è $A(t)^{-1}=B(t)$
        \[ B(t) =
        \begin{pmatrix}
        \dfrac{1}{\norm{\dot{c}(t)}} & * & \cdots & * \\
        0 & \dfrac{1}{\norm{\tilde{e}_2(t)}} & \cdots & * \\
        \vdots & \vdots & \ddots & \vdots \\
        0 & 0 & \cdots & \dfrac{1}{\norm{\tilde{e}_j(t)}}
        \end{pmatrix}
        \]
        \[ e_j(t) = \sum_{r=1}^{j} b_j^r(t) c^{(r)}(t) \]
        Sia $i \leq n-2$ 
        
        $\displaystyle e_i(t) = \sum_{r=1}^{i} b_i^r(t) c^{(r)}(t)$

        $\displaystyle \dot{e}_i(t) = \sum_{r=1}^{i} \dot{b}_i^r(t) \textcolor{blu}{\overbrace{\textcolor{verde}{c^{(r)}(t)}}^{\perp e_{i+1}(t)}} + b_i^r(t) c^{(r+1)}(t)$
        \begin{align*}
        \omega_{i,i+1} (t) &= \dot{e}_i(t) \cdot e_{i+1}(t) = b_i^i(t) c^{(i+1)}(t) \cdot e_{i+1}(t) = b_i^i(t) \left(\sum_{r=1}^{i+1} a_{i+1}^r e_r(t)\right) \cdot e_{i+1}(t) \\
        &= b_i^i (t) \left(\sum_{r=1}^{i+1} a_{i+1}^r \delta_{r,i+1} \right) = b_i^i(t) \, a_{i+1}^{i+1}(t)>0 
        \end{align*}
        Con $\displaystyle i \leq n-2 \iff i+1 \leq n-1 \Rightarrow k_i(t) = \frac{\omega_{i,i+1}(t)}{\norm{\dot{c}(t)}} >0$ 
        \end{proof}